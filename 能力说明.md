# handglyph 能力说明（v0.7.0）

> 客观描述当前代码实际具备的能力、边界与关键参数。凡未验证的，文中标注"未实测"。

## 一、定位

给一张包含文字的图片，提取其中的文本，确认要写的内容，再用**使用者本人**的手写风格重新书写到**使用者自选的纸面背景**上，输出一张新的图片。

三步：

1. **文本来源**：直接输入文本；或给一张图，由 `extract` 提取文本（本机 OCR，可选后端），提取后由人工确认、修正。
2. **笔迹来源**：使用者本人提供的字版照片，经 `build` 切分入库，成为该用户的字形库。**不使用字体文件**，每个字的笔画都来自本人手写。
3. **输出**：由使用者从内置纸面中选择背景，`render` 排版合成，输出新的图片。

补充约束：

- **不依赖在线服务**，全程本地计算，不使用生成式模型，不产生 AI 水印。
- **背景由使用者选择**，内置 5 张纯纸（白纸 / 横线 / 方格 / 红格 / 米黄笔记本）；不支持自带背景（设计取舍：自带背景会显著提高合成要求与失败率）。
- 与同类工具（Handright、text-to-handwriting、handwriting-web）的区别：那些是"字体渲染"，本项目是"本人字形的提取与重组"。

## 二、子命令与数据流

| 子命令 | 作用 | 输入 | 输出 |
|---|---|---|---|
| `build` | 字版照片 → 字形库 | 照片、字符顺序 | `library/glyphs/<码位>/NN.png`、`manifest.json`、`atlas.png`；加 `--debug` 另出 `build_debug.png` |
| `build --form-sheet` | **补字单专用**切分（按格子取字） | 补字单照片、字符顺序 | 同上；只收格内的手写墨迹，表单上的打印文字一律丢弃 |
| `atlas` | 切分图鉴升级为**改名逃生门** | 字库、`序号=字` 映射文件 | 重写 `manifest.json` 的字符映射（`--dry` 只试算） |
| `extract` | 图片 → 页面描述骨架 | 图片 | `page.txt`（OCR 提取；`--backend auto/tesseract/paddle`，后端缺失时写出待填骨架并提示） |
| `coverage` | 检查缺字 | 文字、字库 | 缺字清单（按出现频次排序）+ 提示命令 |
| `form` | 生成补字单 | 缺字清单 | 田字格 PNG（含 6 条采写规范；说明文字与参考字均为**浅灰**，不会被当笔迹） |
| `quality` | 字库质检 | 字库 | `library/quality.txt`：逐字指标、需重写清单、**形状校验段** |
| `shape` | 形状校验：排查被压扁的字形 | 字库 | 宽高比异常清单 + 重写提示（**不自动改字形**） |
| `doctor` | 环境自检 | — | Python 版本、三个核心依赖、可选组件、中文字体、5 张背景图、字库状态与实例档案摘要、使用提示 |
| `selftest` | 自检：跑内置回归测试 | — | **70 处**断言（静态 `check()` 调用数，见 7.2） |
| `calibrate` | 校准判据阈值 | 字库 | `library/metrics.json` |
| `prune` | 淘汰字库里的差实例 | 字库 | 删除离群实例、刷新 `manifest.json` |
| `render` | 排版并合成 | 页面描述、字库、纸面 | 成品 PNG + 同名 `.audit.txt` 质检报告 |

闭环：`coverage` 报缺 → `form` 出单 → 手写回传 → `build --form-sheet --merge --replace` 并库 → `render` 出图并审计。

**补字是分次的，字库因此分批**：每次 `--merge` 补字都会开一个新批次（`b1`、`b2`…），批内各字轻重一致，批间可能不同。`batch` 子命令把"哪一批跟别的批不是一路粗细"算出来；`render` 在批数 ≥2 时也会自动在末尾提示。详见 4.5。

⚠️ **补字单必须带 `--form-sheet`**：补字单是程序生成的规则网格，上面的说明文字、参考字、格线都是**印刷内容**。不加 `--form-sheet` 时，切分器会把它们当成笔迹——实测空白补字单能切出 6 个"字形"（全是说明文字碎片），填了字的补字单反而把手写字当离群值丢掉、入库 0 个字符。加 `--form-sheet` 后按格位取字，实测两种情况都正确。

## 三、页面描述语法

```
* 1 . 1V            ← 星号行：小号标题（默认字号 34）
1) F^1 = B~ · C      ← ^ 下标，~ 给后一个字加上划线
> 这一段整体缩进两格   ← 行首 > 表示该行缩进（> 自身不占字位）
#wave                ← 插入一条手绘波形
#gap 60              ← 插入 60px 空白
```

- 文件按 **`utf-8-sig`** 读取：Windows 记事本默认存的带 BOM 的 UTF-8 也能正确识别首行 `*`（否则 `\ufeff` 会让标题判定失效）。
- 超宽的行默认**折到下一行**（悬挂缩进），不再整行压缩；`--no-wrap` 恢复旧的"整行等比缩小"行为。

## 四、关键参数（默认值）

### 4.1 字版切分（`build`）

| 参数 | 默认 | 说明 |
|---|---|---|
| 墨迹阈值 | 局部背景 − 25 | 与局部背景（均值滤波 81px）差分，抗阴影与纸纹 |
| 去横线 | 纵向膨胀 3px → 横向开运算 80px → 减 9px 带 | 去横线纸的格线 |
| 最小面积 | 20 px | 滤噪点 |
| 整行宽 | 丢弃宽 > 0.4 × 图宽 | 滤贯通横线 |
| 细长块 | 丢弃 宽 > 2.5 × 高 且 高 < 18 | 滤线头、短横 |
| 面积离群 | 保留 中位面积 0.25–4 倍 | 滤碎块与巨块 |
| 实心块 | 丢弃 填充率 > 0.55 | 滤墨点 |
| 字号一致性 | 保留 高 0.55–2.0 × 中位高 | 滤打印抬头、标点 |
| 字符数校验 | 与 `--expect` 长度不符即告警 | 防止命名静默错位 |

**读图前置：EXIF 方向**。`build` / `extract` 统一经 `ImageOps.exif_transpose` 读取，按照片的 EXIF `Orientation` 标记把像素真正转正并删掉标记。手机竖拍的照片几乎都带旋转标记，不加这一步会让行分组、去格线等"假设图片正立"的环节全部失效；对本来就没有标记的图是空操作。

### 4.1b 补字单切分（`build --form-sheet`）

补字单是**程序自己生成的规则网格**（几何已知），所以不用上面那套"猜连通域"的通用切分，而是直接定位格子。

| 参数 | 默认 | 说明 |
|---|---|---|
| 列数 | `--cols` 12 | 补字单列数（`form` 与 `build --form-sheet` 两端都有该参数，**必须同值**，否则格位对不上） |
| 格边长 | `CELL = 108` px | 唯一真源常量。`make_form` 画格、`build --form-sheet` 兜底、图鉴排版、残片闸门共用；`GRID_PERIOD_*` 也以它为单位 |
| 格阵起点 | `FORM_X0 = 90`、`FORM_Y0 = 560` | 找不到格线时的兜底定位 |
| **格线定位** | 局部自相关找周期 + 折叠相位法定相位 | **不用投影贯穿线**：稀疏补字单上竖格线总长才 200 多像素，投影法会被说明文字行、十字中线带偏（实测列步距认成 27.5px，真值 108px） |
| 背景场 | `uniform_filter(size=41)` | 格线是浅灰打印（150~205 灰），墨迹阈值 25 的二值化后整条消失（实测 colsum 最大仅 75），必须用局部对比找 |
| 交叉校验 | 行列周期差 ≥ 1.5 倍即丢弃较小者 | 说明文字行距也是 40px，会与行周期混淆；格子是方的，行列周期必须接近。**用 ≥ 而非 >**：实测周期恰为 60 对 40（正好 1.5 倍）时，严格大于会把两个错周期一起放过 |
| **周期合理性带宽** | `GRID_PERIOD_LO/HI = 0.75 / 1.35` | 实测周期必须落在 `cell × [0.75, 1.35]`（即 81~146 px）内，否则**否决并回退内置几何**。这是防"静默污染"的关键一道：自相关对**非格线的强周期**一样敏感，说明文字行距就是现成的反例（实测误检成 40/50/60 px），而单边检出时交叉校验管不到它 |
| 格内缩进 | 8% | 避开格线本身 |
| **墨深门槛** | `FORM_INK_DEPTH = 100` | 格内平均墨深 > 100 判为空格。手写墨色约 25~60 灰，说明文字 165 灰、参考字 200 灰，一刀切干净 |
| **残片闸门** | `FORM_FRAG_SPAN = 0.35` | 格内墨迹高度跨度 < `0.35 × cell`（即 <38 px）判为"被误检窗口切断的残片"，丢弃并计数。**判据锚在已知格宽上，不是窗口高**——依据见下表 |
| 格外组件 | 一律丢弃 | 说明文字、标题、格线碎片都在格外 |
| 字符顺序 | **格位顺序即字符顺序** | `--expect` 是**必填**（缺了直接退出码 2）：字符名只能来自它，否则程序只能编造 `u00xx`。给了之后按格位报告缺了哪些字 |
| 调试 | `--debug` | 黄框＝检出的格位，绿框＝实际入库 |

**残片闸门的判据为什么锚在格宽上**（实测，同一个"填了字的补字单"）：

| 场景 | 窗口内高 | 墨迹高度跨度 | 跨度 / 窗口高 | 跨度 / 格宽 108 |
|---|---|---|---|---|
| 行周期误检 50px（窗口被压扁） | 42 | 20 | **0.48** | **0.19** |
| 几何正确 | 92 | 56 | 0.61 | **0.52** |

- 若按"跨度 < 窗口高一半"判：0.48 对 0.50 —— 只差 4%，能不能拦住**取决于误检周期碰巧落在哪**（窗口稍高一点就不触发了），不可靠。
- 按"跨度 < 0.35 × 格宽"判：0.19 对 0.52 —— **两倍多余量**，两侧都不贴边。

**周期误检的失败模式与实测后果**（这是先修周期、再留残片闸门的原因）：

| 环节 | 干预前 | 干预后 |
|---|---|---|
| `detect_grid` 返回 | `cp=None, rp=50`（真值 108/108） | `cp=None, rp=None`（回退内置 108） |
| 格位窗口 | 108 × 50（把字拦腰切断） | 108 × 108 |
| 入库字形 | 与 53×**49**、门 52×**49**（残片） | 与 53×**58**、门 30×**58**（完整） |
| 用户看到的输出 | "入库：2 个字形 / 2 个不同字符"，**看起来完全成功** | 同上，但字形是对的 |
| 后果 | 残片静默进库，之后每次 render 这两个字都是坏的 | — |

⛔ **为什么这个 bug 比报错更糟**：残片通过了墨深门槛、像素数下限等所有既有检查，被正常命名入库，输出没有任何异常信号。用户要到出图时才会发现字是坏的，而那时已经无从追溯是哪次补字单出的问题。

**实测对照**（合成补字单，顶部说明文字故意用老的危险深灰 70 灰）：

| 场景 | 旧路径 `build` | `build --form-sheet` |
|---|---|---|
| 填了 2 个字 | 入库 4 字形 / **0 个字符**（打印污染，手写被丢） | **入库 2 字形 / 字符集正确** |
| 空白单 | 入库 4 字形，字符集**全是说明文字碎片** | **0 字形，干净拒绝** |

⛔ **旧版打印件必须作废重打**（v0.1 之前打印的补字单）。

`form` 在 v0.2 把说明文字/参考字改成了 165/200 灰，都高于墨深门槛 100。但**旧版打印件里参考字是 70 灰 —— 比门槛还深**，于是它会被当成手写笔迹收进库。实测（旧配色表单、一个字没写）：入库 与 53×58、门 30×57，两个**完整的参考字**，输出显示"有手写墨迹 2 格 / 入库 2 个字形"，没有任何异常信号。

这道坎**无法用灰度阈值解决**：旧参考字 70 灰与"下笔很轻的手写"70 灰在像素层面完全同值，任何阈值都会误伤一边。可行的判据只剩"位置一致性"（印刷参考字在每个格子里都落在**同一相对位置**、墨色方差极小，而真实手写的位置和力度必然有抖动），但那需要新的实测标定，暂不做。

**实操**：补字单一律用**当前版本**的 `form` 重新生成再打印；手上已有的旧打印件直接作废。若确实要用旧件，拍照时把说明区裁掉、并只拍你写了字的那些格子。

`form` 生成端同步加固：标题 120/150 灰、说明文字 165 灰、参考字 200 灰、格线 120 灰、中线 205 灰 —— 全部落在 `FORM_INK_DEPTH` 之上。

### 4.2 字形入库

| 参数 | 默认 | 说明 |
|---|---|---|
| alpha 阈值 | 0.16 | 低于此值视为纸面 |
| 峰值归一 | a / (0.45 × 峰值) | 统一笔画浓度 |
| 清晰度打分 | 0.45×字号 + 0.25×实度 + 0.15×填充 + 0.15×锐度 | 同字多实例取优 |
| 弱字形阈值 | 分数 < 0.62 | 列入"建议重写" |
| 保留实例 | 每字最优 2 个 | 供轮转使用 |

**实例档案**：`manifest.json` 的 `instances` 字段逐条记录 `{"score", "src", "date", "seq", "batch"}`。
- `src` / `date`：这张字形来自哪张照片、哪天入的库。
- `seq`：**全局唯一序号**，就是 `atlas.png` 上标的编号。它与文件名解耦 —— 文件名在各字符目录下都从 `00.png` 开始，天然重名，不能拿来当身份。`doctor` 会汇总"来源张数 + Top3"。
- `batch`：这份实例属于哪个批次（`b1`、`b2`…），见 4.5。

**批次档案**：`manifest.json` 的 `batches` 字段逐批记录 `{"id", "src", "date", "chars", "stroke_med"}`。`stroke_med` 是该批字形的**中位笔画半径**（折算到 40px 字高），用于批间比对。老字库没有这个字段，`batch` 会以 `--merge` 补字时建立的批次为准。

### 4.2b 改名逃生门（`atlas --fix`）

`build` 的自动命名只看"切出几块、顺序对不对"，一旦切分或顺序出错，整批字的名字会一起错位，以前只能重拍重跑。现在可以救：

```bash
handglyph atlas --lib library                  # 出切分图鉴，对着图核对
# 写出映射文件 map.txt：
#   3=确
#   7=能
#   3-6=确能控制        （或是写成连续区段）
handglyph atlas --lib library --fix map.txt --dry   # 试算，不动文件
handglyph atlas --lib library --fix map.txt         # 真改
```

- 改名实现是**搬目录**（字形按 `ord(字)` 建目录，改名＝换目录名）。
- 定位优先用实例档案里的 `seq`；老字库没有 `instances` 时退回文件名，此时映射键要用 `字符/序号` 形式（如 `确/1`）。
- **目标目录已有同名文件时不放弃**，改用跳号命名（`00.png` → `01.png` → `02.png`）—— 不同来源的同名字形在一个字下并存是正常的，多写几遍正是字库该有的样子。此前的实现在这种情况下会静默跳过，导致"已改名 0 个实例"。
- 改名后自动清掉失效的孤儿档案与 `metrics.json` 质检缓存，并提示接着跑 `clean` + `quality`。

### 4.3 渲染（`render`）

| 参数 | 默认 | 说明 |
|---|---|---|
| 字号 | 40 px（标题 34） | `--stretch/--rotate` 之外的主要观感开关 |
| 拉丁缩放 | `--latin` **0.95** × 汉字高 | 字母/数字按大写高对齐汉字。**文档曾误记为 0.74**，代码实测为 0.95（常量 `LATIN` 与 CLI 默认值一致） |
| 字距 | 0.22 em ×(0.85–1.2 随机) | |
| 行距 | 1.8 × 行高 + 10 px | |
| 超宽处理 | **折到下一行**（悬挂缩进） | `--no-wrap` 恢复旧的"整行等比缩小" |
| **行基线漂移** | `--drift` **3.0 px** | 整行叠加一条低频正弦（周期约 2.6 字宽），让每行不是用尺子画出来一样平；`--drift 0` 关闭 |
| 段落缩进 | 行首 `>` → 缩进 2 个字宽 | `>` 自身不计入字数、不占字位 |
| **布局与绘制** | **同一次遍历** | 量宽和贴字共用同一条排字函数：此前量宽用独立重置的随机源、贴字用连续随机源，两边状态不同导致**测量宽度 ≠ 实际宽度**，且量宽路径会对字形**重复做一次形变**。现合并为一次布局 + 一次绘制 |
| 纸面约束 | 逐行左右边界 + 逐列底边 | 纸面轮廓由 `(亮度>150 且 色差<60)` 最大连通域求得 |
| 笔画加长 | `--stretch` 0.14，概率 **45%**、长度 **0.55–0.95** 倍 | 沿单一主方向平移叠加并衰减，模拟出锋。⚠ 算完**按墨量归一化**（见下） |
| 整字旋转 | `--rotate` **0.35°** | 刻意压到极小。手写倾角集中在 ±1.5° 内，超过 1.5 就能逐字看出歪 |
| 竖画扶正 | `--lean-tol` 1.2°、上限 `--lean-cap` 3.5°、置信 `--lean-gain` 1.0 | **默认开启**。判据＝"长竖画总长最大化"（只累加长度 ≥ 0.35 字高的竖直游程），峰位即倾角。三重闸门：置信度≥gain、倾角>tol、幅度≤cap，任一不过就不动。`--lean-tol 0` 关闭 |
| 非刚性形变 | 位移场 ±1.6 px | 让不同部位的笔画各自偏移 |
| 局部粗细 | `--stroke` 倍数（默认 1.0） | **微妙档**：噪声场去均值后乘 `1 + 0.06·max(f,0)`，再顺着 f 正负把"淡的提浓、浓的减淡"（0.88/0.90）。**不向外扩张轮廓** —— 老做法往字形外糊一圈边缘像素，实测两遍墨量差 12.3%，一眼可见，已被断言拦下 |
| ~~笔画宽度归一~~ | **已删除** | v0.7 曾用 `--stroke-norm` 做"亚像素级定向加粗/变细"，**三版全废**，已于本轮整块移除（函数、常量、CLI 参数、4 条断言全清）。删它不是没写完，是**这件事在离散数据上做不到**：细笔画（本库多数，半径 1~2px）做一次腐蚀即整条消失；粗笔画只能整格跳；连 `stroke_radius` 本身在 2px/3px 上都量出同一个 1.0。现在**粗细一律不动**，只归墨色浓淡。详见 §4.6 与「笔画宽度归一为什么删掉」 |
| 量行高的宽度口径 | 与贴字**共用** `token_metrics()` | 估算是"字形真实宽高比 × 字号 + 字距期望值 × 1.10 余量"。v0.7 前估算用一张固定倍率表且**漏了字距项**，实测低估 10~22%，分页因此偏乐观 |
| 尺寸抖动 | 宽/高 **0.985–1.015** | 等比缩放，墨量跟着面积走，所以 1.5% 边长差 ≈ 3% 墨量差 |
| 垂直抖动 | ±2 px | |
| 同字复用间隔 | 200 px | 同页内过近则换实例 |
| 墨色 | RGB(32,32,36) | |
| 收口模糊 | 0.15 px | |
| 波形 | 线宽 2/3 px 随机、每点 ±0.9 px 抖动、脉冲宽 0.07–0.34、电平 ±2.5 px | |
| **标点** | **优先用字库里的字形**，字库没有才程序画点 | 入库的标点是使用者本人的笔迹，比程序画的点更像他写的；程序画点兜底（`。` 画空心圈、其余画实心点），并计入字数对账 |
| 字数对账 | `.audit.txt` 写明"应渲染 X 字（其中程序画点 Y 字），实渲染 Z 字" | 程序画点也进质检记录（标 `prog=True`），但不参与糊/碎/浅判定；缺字跳过、不计入"应渲染" |

#### 观感分工：像不像本人，靠什么

这是 v0.5 定下来的原则，改任何"抖动"参数前先读这一节：

| 靠什么 | 承担多少 | 由谁做 |
|---|---|---|
| ① 换笔迹（用**本人**字库） | 决定性 | 字库本身 |
| ② 字库容量（同一个字多几个实例轮着用） | 很大 | 补字单 `--copies 3` |
| ③ 极轻的旋转与形变 | 小 | `--rotate` 0.35° / `warp` ±1.6px |
| ④ 笔画粗细的轻微浓淡 | **很小** | `--stroke`（微妙档） |

**⛔ 不要靠加大 ④ 来"制造差异"。** 用户 2026-09-17 的原话：
「你这个参数太明显了，就是这个粗细变化幅度太大了，你应该把它调得非常微妙微小。」
「抄多了他心里会厌烦，抄 5 遍的话，他还不如自己写呢。所以我们定的抄 3 遍。」

两条配套规矩：

- **补字单抄 3 遍**（不是 5 遍）。第 4、5 遍人会敷衍，那几个实例质量反而更差，
  等于往字库里掺次品。`make_form` 的提示语里已经写明这一点。
- **粗细只做"这一次落笔重了一点点"**。任何能从成品图上"一眼看出这个字更粗"的幅度
  都是过大的，不达标的改动会被自检的 `粗细扰动实测` 断言拦下。

#### 出锋为什要归一化墨量（实测数据）

`elongate` 是"把字形沿某方向搬运一段再叠上去"，方向不同、墨量增幅差得很远。
同一个 35×34 的字形，`--stretch 0.14` 下单独跑 `elongate`：

| 方向 | 墨量 | 增幅 |
|---|---|---|
| 水平 (0, ±1) | 373 | +18.3% |
| 垂直 (±1, 0) | 392 | +24.6% |
| 对角 (±1, ±1) | 403–417 | +28.1% ~ **+32.3%** |

**光方向选择就带来 14 个百分点的墨量差** —— 比粗细扰动本身大四五倍。
用户看到的"一片字有粗有细"，绝大部分来自这里，而不是来自 `restroke`。
所以 `elongate` 算完必须按 `目标墨量 / 实际墨量` 整体缩一次（目标 = 原墨量 × 1.12）。
缩的是**整体不透明度**，外形分毫不改 —— 出锋的尾巴还在，只是淡下去，
正好就是真实书写里"收笔那一带墨越来越干"的样子。

归一化前后，同一 seed 连写 40 次的墨量离散度：

| | 均值 | 标差 | 极差 |
|---|---|---|---|
| 归一化前 | 316 | 30 | **40.8%** |
| 归一化后 | 311 | 24 | **28.4%** |
| （关掉出锋作对照） | 300 | 15 | 26.3% |

### 4.3b 自动排版（`render --flow`）

**解决什么问题**：一页写完、内容只占上半页时，下半页是一大片空白。手工做法是把下一张的
内容剪上来，但那是靠眼睛看、靠手搬。`--flow` 让程序自己判断并做这件事。

**两步走**：

1. **量**（不落墨）：每行按 `measure_line_span` 算出纵向跨度（含折行占的行数）。
   ⚠ 这一步**不能**借用 `_render_line` —— 那会推进 picker 的随机状态，
   导致"量一遍、画一遍"两次结果不一致（行高变了，排版就对不上）。
2. **装**（贪心装箱）：`plan_pages` 逐行累加，超过页高就翻页。
   贪心而非均分 —— 均分会让"3 行 + 1 行长行"变成每页各 2 行，长行那页照样留白。

**关键参数**：

| 参数 | 默认 | 说明 |
|---|---|---|
| `--flow` | 关 | 打开自动排版 |
| `--flow-slack` | 240 px | 页尾空白容忍上限。只有超过它才算"大片空白"；240px ≈ 两行正文，低于这个数说明页面本来就排满了 |
| `FLOW_MIN_GAP` | 300 px | 判"空白段"的最小高度。更矮的空隙是行距自然形成的，不算 |

**输出**：

- 单页时就是 `-o` 指定的文件；多页时自动加后缀 `_p1`、`_p2`…
- 每页都是**同一张纸面模板的完整拷贝**（纸纹/边缘都在），不是"半张纸"
- 审计报告逐页写明：本页几行几字、正文占多少 px、页尾空多少 px（游标值 + 实测值），
  以及 `blank_bands` 扫出的空白段坐标

**空白段检测**（`blank_bands`）：逐行统计墨迹比例，连续无墨且高度 ≥ `FLOW_MIN_GAP`
的区段即为空白段。为什么按行统计而不是找连通域 —— 文字是逐字贴上去的，
连通域会把一个字拆成几块、又会把相邻行连成一片，判"哪里是空白"反而失准。

**两条口径要分清**：

- **游标尾空**：按排版游标算的剩余空间，反映"我打算留多少"
- **实测尾空**：拿成品像素复核（`blank_bands` 的末段），反映"实际留了多少"

两者不一致说明有行没落墨（落点出画布、或纸面量错）—— 报告里两个数并列，就是为了让这种
不一致一眼可见，而不是信游标报"已压到阈值内"。

**已知边界**：单页内容本身就不满时（比如只有 5 行），`--flow` 无能为力 ——
没有"后续内容"可以提上来。报告会如实写"仍偏大"，不会假装填满了。

### 4.4 质检指标与判据

字形指标（对任意字库、任意人成立，无需人工拧参数）：

| 指标 | 定义 |
|---|---|
| `sharp` 锐度 | 边缘像素 / 笔画面积 |
| `soft` 发虚 | 半透明像素(0.18–0.82) / 实心像素 |
| `frag` 碎片 | 连通域数 / 每千墨点 |
| `solid` 实度 | 实心像素的平均 alpha |
| `fill` 填充 | 墨迹 / 外框面积 |

- **分组**：cjk / latin / punct 三组各自建基准，不混比。
- **阈值**：组内 `中位数 ± 1.4826 × MAD`，`--k` 默认 **3.0**（越大越宽松）。
- **方差下限**：sharp 0.08、soft 0.02、frag 0.5、solid 0.01、fill 0.02（防 MAD=0 时判据爆炸）。
- **出图审计**：判据**按字类分别取基准**（汉字 / 字母数字 / 符号三组，每组 ≥4 字才单独建基准，否则退回全页中位）。同组内 `密度 > 1.75×组中位` 或 `锐度 < 0.55×组中位` → 判"糊"；`密度 < 0.05×组中位` → "碎"；`密度 < 0.03` → "缺"。审计报告会逐组印出基准值与判据值。

#### ⛔ 为什么判据必须按字类分组（2026-09-17 实测，v0.5.1 修）

出图审计原先**全页取一个中位当基准**。实测一页 407 字，报出 22 处"不合格"，但把渲染器自己的记录（`dens` / `sharp`）按字类拆开看：

| 字类 | 字数 | dens 中位 | 越过 dens 判据 | 合计报警 |
|---|---|---|---|---|
| 汉字 | 7 | 0.277 | 3 | 3 |
| 字母数字 | 237 | 0.239 | **17** | **18** |
| 符号 | 163 | 0.191 | **0** | 2 |

**字母组 17 个越线、符号组 0 个** —— 两组字数相近（237 / 163），差异不可能来自"字写得好不好"。真因是渲染里拉丁字母走 `LATIN = 0.95`（缩到 0.95 倍字号），笔画在**小一号的框**里相对更宽 → `dens` 系统性偏高、`sharp` 系统性偏低（`sharp` 的分母是笔画面积，笔画越粗值越低）。全页一个基准，等于**拿两类不同尺度的字互比**。

证据链（排除"字本身坏了"）：
1. 逐个查被报的字形，`E`（77×129，墨点 2582）放大看**干净完整**，无残笔、无格线；
2. 全库 57 字按渲染口径复算，预测的报警集合是 `·相-→`，与实际 `BEA⊕=` **对不上** —— 说明单靠"字形属性"推不出这个集合，它依赖页面里有哪些字；
3. 同一字形在不同尺度下测：`C` 与 `c`（同形、同尺寸）给出**完全相同**的 `dens` / `sharp`（0.137 / 0.714），证明这是一条纯几何量，与"是谁写的"无关；
4. 把字库字形按渲染口径（贴版 + 墨量归一）复算后按字类分组：汉字组 0 个越线、字母组 17 个 —— 分组能干净地把误报摘掉。

修法：`_audit_class(ch)` 分 **cjk / latin / punct** 三组，各组用**自己的中位**当基准；某组不足 4 字时退回全页基准（1~2 字时中位就是它自己，永远判"正常"）。修后同一页报警 **22 → 12**，且剩下的全是**组内真离群**（`E`/`⊕`/`A`/`B`/`=`）。

⚠️ 分类边界取 **CJK 码点区**，不是 `isascii()`：`⊕`（U+2295）、`→`、`·` 都不是 ASCII，但渲染尺度与标点相同；按 `isascii()` 一刀切会把它们归进"汉字组"当基准 —— 实测 `⊕` 就是这么混进去的。

**形状校验（宽高比，独立于上面五项指标）**：上面五项都测不到"字形被压扁"这类缺陷，故单列一项。

| 项 | 值 |
|---|---|
| 参照来源 | 系统字体（`simhei.ttf` 优先）渲染该字，量墨迹框的高/宽 |
| 判据 | `实际宽高比 / 参照宽高比 ≥ SHAPE_RATIO_LIMIT`（默认 **1.7**） |
| 适用范围 | **仅汉字**（ASCII 与符号在手写中比例本就与印刷体差得远，实测 `-` 误报 2.37 倍） |
| 最小尺寸 | 任一边 < 12px 不判（量不准） |
| 处置 | **只报告 + 出补字单，绝不自动拉伸** |
| 触发点 | `shape` 子命令、`quality` 报告末尾、`build` 入库时自动跑一遍 |

⛔ **为什么不做自动修复**：裁窄时丢掉的横向细节已经不存在，横向拉伸只是插值，会把笔画拉胖。实测把 `确` 从 31px 拉到 59px 后变成一坨糊块，比原状更差。唯一可行的是重写。

⚠️ **已知的误报**：书写者某个字天生偏窄（实测本库 `门` 1.80 倍）会与真压扁的字一同被抓。阈值无法干净分开二者，故设计为**只报告、由人判断**。

**实测数据（个人 57 字库）**：按渲染口径（贴到 40px 高 + 对比度拉伸）复算，全库 79 个实例的 `dens` 中位 0.215 / P90 0.297，`sharp` 中位 0.734 / P10 0.540。分字类看：汉字组 0 个越线、字母组 17 个、符号组 0 个（分组前）。分组后第 1 页 301 字报警 9 处、第 2 页 111 字报警 3 处。

### 4.5 字库分批与批间粗细比对（`batch`）

补字是**分次写**的：一次写的字（同一支笔、同一天、同一种状态）内部轻重一致，换一次写就可能整体偏粗或偏细。所以字库按**批次**记账，并支持批间比粗细。

**批次怎么产生**：

| 操作 | 批次行为 |
|---|---|
| `build`（不带 `--merge`） | 清空旧批次，本批记为 `b1` |
| `build --merge` / `--merge --replace` | **开新批次** `bN`（N = 现有批数 + 1） |
| `build --rebuild` | 推倒重建，批次清空重来（不留幽灵批次） |

**关键参数**：

| 参数 | 默认 | 说明 |
|---|---|---|
| `BATCH_STROKE_REF_H` | 40.0 px | 粗细比对前先把各字形折算到此字高（与正文字号一致） |
| `BATCH_STROKE_TOL` | **0.20**（即 ±20%） | 批间偏差超过它就判"偏离过大"，提示重写 |
| `BATCH_MIN_CHARS` | 4 | 一批至少这么多字才单独建基准 |

**判据是"批间比中位数"，不是"全库取一个中位再逐字比"**：后者会把一整批的偏移摊成"每个字都异常"，看不出真正的问题。

```bash
handglyph batch --lib library             # 列各批次 + 批间偏差
handglyph batch --lib library --tol 0.15  # 收紧到 ±15%
```

实测输出（桌面 57 字库，两批）：

```
批间粗细比对（参考字高 40px，基准 0.80）
  批次     日期               字数       中位半径       与基准差  判定
  b1     2026-09-14       52       0.91      13.6%  正常
  b2     2026-09-17        5       0.69     -13.6%  正常

各批次粗细都在容忍带内（±20%），无需重写。
```

超限时报告会给出**重写清单 + 重写方法**（`form` 出补字单 → `build --merge --replace` → `clean`），因为这是用户拿到后唯一能照着做的动作。`render` 在批数 ≥2 且超限时也会在末尾自动提示，退出码置 1。

⚠️ **只提示、不自动改**：程序改粗细是把笔画缩放，会把字弄得不像是本人写的；而"重写一遍"能真正根治。

⚠️ 老字库（在分批功能之前建的）没有 `batches` 字段，`batch` 会回答"还没有分批信息"，并在下次 `--merge` 补字时自动建立。若想给老库补上批次（不重切字形），见 §附：本轮修复里的升级方法。

### 4.6 补字单提示语（`form`）

补字单上的采写规范含 **"书写要求"** 小节，其中明文写：

> **每个字的笔画粗细要尽量一致**（落笔轻重要均匀），不要有的字粗、有的字细，也不要一个字里一半粗一半细。

这条是**从源头**减少"批内不齐"的手段。**程序端现在不做任何粗细修正**（试过三版，都删了），所以"齐"完全靠写的时候一致。其余 6 条见 4.1b。

**程序端为什么不管粗细**：v0.7 试着用 `--stroke-norm` 把贴版笔画宽度朝全篇中位拉（亚像素级定向加粗/变细），**实测三版全无效**，已整块删除。原因写在下一节，一句话是：**细笔画不可修（腐蚀一次就没了）、粗笔画只能整格跳、量尺在细笔画上还看不出差别**。程序现在只归"墨色浓淡"（`ink_normalize`），不动几何。

### 4.7 自己导入背景（`render --paper <图片路径>`）

`--paper` 除了 5 个内置名，**还可以直接给一张图片的路径**：

```
handglyph.cmd render 内容.txt --paper "D:\我的纸.png" -o out.png
```

判据是**这个参数能不能当图片文件打开**（扩展名限 `.png/.jpg/.jpeg` 且文件存在），不是"名字里有没有斜杠"——两种写法都支持：

| 写法 | 走哪条路 |
|---|---|
| `--paper white` | 内置名 → `backgrounds/white_plain.png` 或 `.jpg` |
| `--paper 我拍的纸.png`（当前目录） | 图片路径 |
| `--paper "D:\纸\扫描.png"` | 图片路径 |

#### ⛔ 背景图**只能是纸面**，不能带纸面之外的东西

用户 2026-09-18 明确要求：**允许自己导入背景，但必须把"只能加纸面"这件事说清楚**；并且**明确不要自动校验**（原话：「你不用去添加导入检验，不要做这个东西，你就把这个提示词设计的好一点」）。

⇒ 设计取舍：**不做纸面自动检测**。理由：机器判"这是不是纸面"必然有误判，而误判的代价是"用户明明有可用的图却不让用"。改成**把话说透，让人自己看图判断**。提示语集中在 `paper_bg_notice()` 一个函数里，`doctor` 与报错路径**共用同一份**，不会两处说法不一致。

**要什么样的图**：整幅就是一张纸、纸面铺满画面；平铺正对拍/扫；光线均匀无重阴影；长边 ≥1500px。纸上有淡格线/横线没关系（那本来就是纸面的一部分）。

**不要什么样的图**：带桌面、手掌、书本边缘、笔、水杯等纸面之外的东西。

**用错了会怎样**（两种后果都实测过，不是推测）：

| 后果 | 触发条件 | 程序会不会报警 |
|---|---|---|
| **字只写在一角、大片空白** | 图里混进桌面/手掌，那块被判成非纸面 → 可用宽度跟着变窄 | ❌ **不会**。纸面轮廓是量出来了的，程序以为一切正常，用户只会看到一张怪图 |
| **字跑到纸外、贴到桌子上** | 纸面轮廓**压根量不出来**（画面太暗、纸面被遮去大半）→ 排版退回保守宽度 `PAPER_FALLBACK=(40,1200)` 硬排，不再知道纸的边界 | ✅ 会（打印"没能量出纸面可用范围"告警），但那时已经排坏了 |

实测复现方式（`gen_out/swap3/_demo_paper_desk.png`）：造一张"左侧 2/3 是横线纸、右侧 1/3 是木桌面"的图跑 render —— 正文只占左侧纸面那一块，右半边整页空着，**且无任何告警**。这正是"字只写在一角"那一栏。

#### 报错路径分三种（原先会崩）

原来 `--paper` 用了 `choices=`，自定义路径直接被 argparse 拦掉；而错误分支里写的是 `PAPERS[a.paper]`，一旦放行就会 `KeyError`。现改为：

| 输入 | 处理 |
|---|---|
| 内置名但背景文件缺失 | 提示"应在 backgrounds/ 下放 X.png"，并指路 `doctor` |
| 图片路径但文件不存在 | 提示"检查路径有没有写错、文件是不是被移走了" |
| 既不是内置名、也不是图片 | 列出可用内置名，并提示"也可以填图片路径，但必须整幅都是纸" |

三种都**跟着打印一遍 `paper_bg_notice()`** —— 出错的时候正是用户最需要读到这段说明的时候。

## 五、纸张与输出

- 内置 **5 张纯纸**背景（无手、无杂物）：`white` 白纸、`lined` 横线稿纸、`grid` 方格纸、`redgrid` 红格稿纸、`cream` 米黄横线笔记本。
- **也支持自己导入背景**（`--paper <图片路径>`，v0.6.0 起）。但导入的图**必须是纸面**——带桌面/手掌会让字排到纸外或挤在一角，详见 §4.7 与 `doctor` 的说明。**程序不做纸面检测**（刻意为之，见 §4.7 的取舍说明）。
- 输出 PNG；页面尺寸由背景图决定（内置纸 1240×1754）。

## 六、已知不足（客观）

1. **自由字版的命名仍不够稳**：`build --expect` 按阅读顺序命名。**补字单路线已在 v0.2 解决**（`--form-sheet` 按格位取字，顺序由几何决定，实测空白单干净拒绝、有字单正确入库）。但**自由手写字版仍按顺序命名**：横线纸照片实测未通过（87 连通域 → 保留 6，实际 11 字）。已定位原因：**面积离群过滤的中位数是在被杂块污染的全集上算的**。**建议：需要精确入库时一律走补字单 + `--form-sheet`，自由字版只用于快速起步。**
2. **合并后需手动清理**：`--merge` 只加不减，旧的低质量实例仍在库中参与轮转。已提供 **`prune`**：按质检指标淘汰离群实例、保证每字至少留一个。**`build --merge` 后应跟一次 `prune`**。
3. **符号需预先入库**：`⊕`、上下标、`→` 等要么在字版里写过，要么程序合成。目前仅 `F`、`V` 为程序合成；其余符号若无样本，渲染时会跳过并计入缺字。
4. **字形质量受源样本支配**：源样本潦草时，锐化、取优、加长只能有限改善。程序会点名需重写的字，**修复靠用户重写**。
5. **OCR 为可选后端**：`extract` 依赖本机 `pytesseract` + tesseract 二进制；未安装时不报错，而是写出待填文本骨架并要求人工录入。**项目不自带 OCR 模型**。
6. ~~平台依赖~~ **已在 v0.1 修复**：原先写死 Windows 字体路径（`msyh.ttc`、`simkai.ttf`），非 Windows 直接崩。现改为**跨平台字体自动探测**：按 `sys.platform` 分三组候选（Windows 微软雅黑/黑体/宋体/楷体 → macOS 苹方/华文黑体/宋体 → Linux Noto CJK/文泉驿），Linux 再按 glob 模糊兜底；可用环境变量 `HANDGLYPH_FONT` 覆盖。**全部找不到时降级不报错** —— 成品笔迹不依赖字体（笔画全部来自用户手写），只是补字单上的说明文字显示不出来。
7. **形状校验只能查汉字、且不能自动修**：`shape` 用系统字体当参照比对宽高比，只对汉字有效（字母、符号在手写中比例与印刷体差别太大，实测 `-` 误报 2.37 倍，已排除）。检出后**不提供自动拉伸** —— 裁窄时丢失的横向细节无法恢复，拉伸只会把笔画拉胖（实测 `确` 31→59px 后变成糊块）。**唯一处置是重写该字**。此外，书写者某个字天生偏窄时会与真压扁一同被抓（本库 `门` 1.80 倍），阈值无法干净区分，需人工判断。
8. **竖画扶正有两处固有局限**（默认已开启，但要知道它的边界）：
   - **全局旋转无法同时满足竖画与横画**。实测把竖画扶正 8°，该字横画得分从 320 掉到 0 —— 横画会被带斜。所以只能"小幅、只对真歪的字做"，单次幅度上限 3.5°。
   - **判据依赖"字库里存在长竖画"**。对 `一`、`·`、`=` 这类无长竖画的字形，程序判定"不动"（已实测 100% 不误动）；但字形本身若笔画极少、缩放又很小（如库中 `1` 仅 28×17 px），判据分辨率不足，可能给出偏大的倾角，此时靠 `--lean-cap` 兜住。
   - 现状：真倾斜合成样本（±3～14°）100% 召回且误差 ≤1°；对当前字库 50 字中会改动 15 字。因为本库本身字形倾斜与尺度离散度大，改动是"有的字变好、个别变化不明显"，**不是全库一致提升**。
9. **一页容量有限**：内容放不下时渲染会停止，**已不再是静默截断** —— stderr 会告警，且 `.audit.txt` 首行写明「内容被截断：输入 X 行，本页只渲染 Y 行（Z 字）」。多页输出尚未实现（见第八节）。
10. **`build` 的阈值是绝对像素**：81px 背景滤波窗、80px 横线开运算、20px 最小面积、40px 行分组桶，都隐含「字高约 40~60px」的照片尺度。同一张字版用 4000px 原图和 1600px 压缩图会走不同过滤路径。已知问题 #1（横线纸切分失败）很可能与此相关。修法：标记后先估中位字高，所有常数按比例推导；或把照片归一化到「中位字高 ≈ 参考值」再进主流程。**此项未做**。

11. **补字单的格位数与 `--expect` 长度不一致时会名字错位**（v0.7 实测确认，未修）：
    `build --form-sheet` 的格位总数取 `max(len(expect), cols × nrow)`，其中 `nrow = ceil(len(expect) / cols)`。当 `--expect` 的长度不是 `cols` 的整数倍时，`total` 会比 `--expect` 里的字符数多出「补齐到整行」的那几格，这几格的名字落进 `u%04d` 兜底分支，**可能被当成真字入库**。
    实测：`--expect` 给 164 字、`--cols 12` → `nrow=14` → `total=168`，末 4 格（k=164..167）产出伪字符 `u0164`~`u0167` 并写进 `manifest.json`。
    更麻烦的是**字形与字符名的整体错位**：程序按 `kept` 的顺序取名字，中间只要有格子被判为空（没写、写太轻、或墨迹太扁被残片闸门丢弃），后面的字就整体前移。实测一张 168 格的补字单里实际有墨 156 格、程序只读到 131 格，漏读的 25 格把后面所有字的名字带偏了。
    **规避**：让 `--expect` 的长度正好等于 `cols` 的整数倍（`form` 会按纸面尺寸告诉你一页最多几格），不要写不满一整行就去 build。入库后务必看一眼 `library/atlas.png` 核对编号与字符是否对得上。
    当前提示文案只说"这些字还没写或没识别到"（把格位漂移误报成漏写），**退出码仍是 0**，所以这条不会自己报警。

12. **`quality` 报告里四处数字口径不统一**（v0.7 实测确认，未修）：
    同一份 `library/quality.txt` 会同时出现「需重写 N」「N 个字形不可用」「N 个字形的形状或墨量不对」「N 个字的字形不合格」四个不同数字，差在**统计层级**与**去重方式**：

    | 出处 | 数的含义 |
    |---|---|
    | 表头「字库质检：X 个字符，需重写 A」 | 按**字符**去重的离群字 |
    | 「形状校验：发现 B 个字形不可用」 | 按**字形实例**计数（同一个字多个实例会重复计） |
    | 「字库里有 C 个字形的形状或墨量不对」 | 按**字符**去重的形状异常字，但沿用"字形"这个措辞 |
    | 「有 D 个字的字形不合格，需要重写」 | 上面两类**并集**，也是 `refill_notice` 最后给补字单用的集合 |

    实测同一份报告出现 4 / 51 / 42 / 43 四个数。**要看的是最后一个**（并集），它才是 `form` 命令该抄的字。其余三个是过程量，措辞没把层级说清。

## 七、命令行速查

```bash
handglyph doctor                                  # 环境自检（依赖/字体/背景/字库/实例档案）
handglyph selftest                                # 回归自检（改过代码先跑它）
handglyph extract 题目.jpg -o page.txt            # 图片 -> 页面描述骨架（人工确认）
                                                  #   --backend auto|tesseract|paddle
handglyph build 字版.jpg --expect "与门输入并联" [--merge] [--debug] -o library
handglyph build 补字单.jpg --form-sheet [--cols 12] --expect "与天" \
         --merge --replace -o library             # 补字单必须带 --form-sheet
handglyph atlas --lib library                     # 出切分图鉴核对编号
handglyph atlas --lib library --fix map.txt [--dry]   # 按 序号=字 改名（逃生门）
handglyph prune --lib library [--k 3.0]           # 合并后淘汰差实例
handglyph coverage --lib library -t "要写的中文"
handglyph form "与门输入" -o 补字单.png [--copies 3]      # 每字抄 N 遍（**建议 3 遍**，见 §4.3）
handglyph quality --lib library [--k 3.0]
handglyph shape --lib library [--limit 1.7]          # 形状校验：查被压扁的字
handglyph calibrate --lib library [--k 3.0]
handglyph render page.txt --paper white|lined|grid|redgrid|cream \
         -o out.png [--seed 20260914] [--rotate 0.35] [--stretch 0.14] [--stroke 1.0] \
         [--lean-tol 1.2] [--lean-cap 3.5] [--lean-gain 1.0] \
         [--drift 3.0] [--no-wrap] [--flow] [--flow-slack 240]
handglyph render page.txt --paper lined --flow -o out.png  # 自动排版（多页出 out_p1/p2）
handglyph render page.txt --stroke 0 -o out.png            # 关掉粗细扰动（最保守）
```

### 7.1 运行环境与依赖

| 项 | 说明 |
|---|---|
| Python | 3.9 及以上 |
| 必需依赖 | `pillow>=9`、`numpy`、`scipy`（缺任一个启动时给出 `pip install` 提示后退出，不抛 traceback） |
| 可选依赖 | `pytesseract` + tesseract 程序本体 + `chi_sim` 语言包，仅 `extract` 用；缺 `chi_sim` 是中文识别失败的常见原因，提示语已分别说明 |
| 可选依赖（新增） | `paddleocr` + `paddlepaddle`，装后可用 `--backend paddle`。两条 OCR 路线二选一即可，都不装也能用（手打文字） |
| 中文字体 | 自动探测（Windows 雅黑/黑体/宋体/楷体 → macOS 苹方/华文黑体 → Linux Noto CJK/文泉驿，Linux 另有 glob 兜底），可用 `HANDGLYPH_FONT` 指定；全部找不到也不报错，仅影响补字单上的说明文字 |
| 背景图 | `backgrounds/` 下需有 5 张纸，**支持 `.png` 与 `.jpg` 两种格式**（代码按名查找，先 png 后 jpg） |
| 文本编码 | 页面描述、映射文件、`manifest.json` 一律按 **`utf-8-sig`** 读，兼容 Windows 记事本默认的带 BOM 文件 |
| 照片方向 | 一律经 `ImageOps.exif_transpose` 应用 EXIF 方向标记 |
| 启动脚本 | `handglyph.cmd`（Windows）、`handglyph.sh`（macOS/Linux）。均不写死本机路径。Windows 侧**优先挑装了依赖的那个解释器** —— 本机常并存多个 Python，直接取第一个会误报「缺依赖」；也可用 `HANDGLYPH_PYTHON` 直接指定 |

**分发建议**：背景图原为 PNG 共 9.15 MB，转 JPEG(q=88) 后仅 1.03 MB 且肉眼无差别（成品反而更干净，因 JPEG 去掉了 PNG 的纸面噪点），`render` 的输出从 1.67 MB 降到 1.15 MB。故分发包用 JPEG 背景，本机可继续用 PNG。

**打包出厂门禁**：`pack.py` 在打包前先跑一次**静态检查**，再跑 `selftest`，**任一不过就中止、不出 zip**。

- 静态检查扫的是"模块级常量是否定义在引用它的函数默认值之前"——这类错误的表现是 **import 阶段就 `NameError`**，模块一行都跑不起来，`selftest`（模块内的函数）根本没机会执行。真实来由：把散落的 `108` 收成 `CELL` 时常量放在了 1530 行，而 `build()` 的签名在 1252 行。
- `selftest` 几秒就能覆盖 39 个真实踩坑场景（实测：把 `NO_WRAP` 默认值反转后，rc=1 且未生成 zip）。理由是打包属于"发出去就收不回"的动作。
- 确实要跳过时用 `python pack.py --no-gate`。打包后另有一次**夹带检查**，按目录结构判定 `library/`、`glyphs/` 是否混入（只看文件名会误伤 `handglyph.py`）。

### 7.2 自检与回归测试

`handglyph selftest` 跑内置断言（源码里 **70 处** `check()` 调用），其中有若干处**嵌套在守卫里**（`if fpath:` / `if bg_path("white"):` / `if chars:`），缺中文字体或缺白纸背景时会跳过，**所以实际执行条数随环境变化** —— 本机实测 69 条全过。不要拿一个写死的数字当验收标准，看结尾那行「结果：全部通过。」和退出码。改过阈值或流程后应先跑它，比手工渲染几页看图快得多：

| 断言 | 防的问题 |
|---|---|
| `vscore` 向量化与逐像素实现等价 | 性能优化悄悄改变了计算结果 |
| **模块常量定义早于函数默认值引用** | 常量挪到函数签名之后 → import 就 `NameError`，程序完全启动不了 |
| 编号跳过空洞 | prune 删出空洞后，merge 覆盖幸存的旧实例 |
| 竖画倾角召回（合成 ±3~11° 竖线，小角 ≤3.5° / 大角 ≤2.0°） | 扶正判据失效 |
| build 切出字数与 `--expect` 一致 | 切分错位 |
| build 命名与 `--expect` 一致 | 命名错位 |
| 同 seed 渲染两次逐字节一致 | 引入不可复现的随机源 |
| 注释行不计入渲染 | `#` 提示行被当成正文写进成品 |
| 超页截断写进审计报告 | 内容丢了但用户毫不知情 |
| 字数对账行存在且应渲 == 实渲 | 渲染悄悄漏字/多字 |
| 超长行折成多行 | 折行逻辑回退成整行压缩 |
| 缩进指令 `>` 不计入字数 | 排版指令被当成正文写进去 |
| 读图应用 EXIF 旋转标记 | 手机竖拍照片躺倒导致后续全错 |
| 读取 BOM 文本时吃掉 `\ufeff` | 记事本存的文件首行 `*` 标题判定失效 |
| 补字单：只收手写字，打印内容不入库 | 补字单闭环的核心回归 |
| 空白补字单：干净拒绝、不入库任何字形 | 空白单被切出一堆说明文字碎片 |
| **补字单：带外周期被否决（回退已知格宽）** | 自相关把说明文字行距当格距，窗口被压扁 |
| **补字单：入库字形不是被切断的残片** | 残片静默入库（看着像成功） |
| **补字单：残片闸门拦住被切断的墨迹** | 周期判错时第二层兜底失效 |
| **`atlas --fix` 撞名也能改名（跳号命名）** | 改名静默不落地 |
| **`form --kind symbols` 不抛异常且产图** | `SYMBOLS` 常量缺失导致整条命令崩 |
| **build 未带 `--merge` 时拒绝覆盖旧字库** | 整体覆盖 manifest，旧字形全变孤儿 |
| **build `--rebuild` 明确放行重建** | 拒绝护栏不能把"我确实要重建"也一起拦死 |
| **并入字形后 `metrics.json` 已失效** | 用旧基准判新字，`prune` 误删好字 |
| **`plaus()` 放过单笔画字（一 / 丨 / -）** | 宽高比闸门误杀一整类合法字符 |
| **`blend()` 越界返回未落下、界内返回已落下** | 落点算出画布外却静默丢弃，字数对账还报"相符" |
| **老字库（无 `instances`）拒绝纯数字键改名** | 纯数字序号在老字库上跨字符误改 |
| **补字单缺 `--expect` 退出码非 0** | 静默编造 `u00xx` 名字入库 |
| **空白补字单（本次零入库）退出码非 0** | `build` 跑完一个字都没入库却报成功，脚本里 `&& 下一步` 照常往下走 |
| **墨量过低的字形被判出、正常字形不误伤** | 过小的源字形被过度放大会糊成实心黑块 |
| **shape 的墨量判据能报出符号类坏字形** | 判据只查汉字，`·` 这类坏掉的符号一路静默通过 |
| **画布重构前后墨迹密度一致（相对差 <1%）** | 性能重构悄悄改变渲染语义 |
| **自动排版：量行高与贴字同一把尺（单字推进量落在预测区间内）** | 量用一张近似倍率表、画用实测宽度 → 行高系统性少算（v0.7 修）。判据取**单字推进量**而非折行行数：行数是取整量，每行差一个字会累积成 1~2 行差异，做判据会假失败 |
| ~~笔画归一：4 条~~（**已随功能删除**） | 原为"方向正确 / 不动外框 / 贴版半径朝目标 / 目标等于现状时不动"。**前三条是假绿**：样本用一根人造大竖条（源半径 4.5），而真实字库笔画半径多在 1~2px。换成真样本后立刻失败 → 功能整块删除 |
| **笔画宽度归一已彻底移除（无残留函数/常量入口）** | 防止这功能被"再试一次"加回来（试过三版全废） |
| **笔画宽度归一不该复活：1px 笔画做一次腐蚀即整条消失** | 钉死"细笔画不可修"这个物理事实 |
| **`stroke_radius` 在 2px/3px 笔画上无分辨率** | 当初写出假断言的直接原因：量尺看不出差别，却被当成"半径没动" |
| **笔画标尺按字形高折算**（batch 用） | 分母写成常量 40 → 等于没折算，与 batch 口径不一致 |
| **图鉴是浅底深字（字形框内暗像素 < 50%）** | 图鉴接口把 alpha 当灰度画 + paste 不传 mask → 黑底白字反相图 |
| **倾角缓存命中率 > 80%（键锚在源字形身份）** | 缓存键取"随机扰动后的内容指纹"，命中率恒为 0，等于没缓存且看不出来 |
| **自由字版零入库退出码非 0** | `rc=4` 只挂在补字单路径的 `empty` 标志上，拍空白纸照样返回 0 |
| **--flow 装不下时：rc=5 且报告写明截断** | `truncated` 是死标志：那一页剩余的行不在任何一页上，报告还写"已压到阈值内" |
| **审计报告会点名被裁掉过半的字形** | `rec["clipped"]` 只写不读，"必须程序说"成了空话 |
| **墨量过低的字形：审计标「跳过」并点名** | 文档承诺的"跳过并在审计报告里说明"这条路径并不存在 |
| **metrics.json 损坏时重算（不报"字库为空"）** | 容错读取返回 None 被当成"库是空的"，误导性报错 |
| **补字单容量：列超限 / 字太多（含 copies）都被拒、单字单格正常** | 格子画到纸外 → build 端当空位跳过 → 静默少字 |
| **单笔画字符集含全角引号（不再靠隐式拼接）** | 隐式字符串拼接丢字符，且再补一个 ASCII 引号就 SyntaxError |
| **--stroke 幂等：同进程调两次不累积** | 从"上一次的值"派生 → 第二次调用变成 k²、第三次 k³ |
| **重构后 render 仍能产出墨迹** | 画布重构把整条渲染链搞坏 |
| **`form --copies` 的格位顺序与 `--expect` 串一致** | 重复展开在两处各写一遍，写岔了每个字都被安到错的名字上，而字数对得上 |
| **自动排版：装箱不越界、顺序不乱、能塞就塞** | 装箱装错不会报错，只是内容被挪到别的页/一页塞下放不下的行 |
| **自动排版：量出的行高随内容增长** | 量行高时漏算折行，超高行被塞进页尾、渲染时才落到画面外 |
| **自动排版：成品里的成片空白能被定位** | `blank_bands` 判反了，自动排版就没有判据 |

无中文字体的机器上，涉及字体的断言自动跳过并在输出里标注。

**退出码约定**（脚本化调用时按这个判成败）：

| 码 | 含义 | 触发场景 |
|---|---|---|
| `0` | 成功 | — |
| `1` | 自检失败 | `selftest` 有断言不过；`coverage` 发现缺字 |
| `2` | 用法或环境错误 | 参数写错、缺 `--expect`、字库目录不完整、主动拒绝（`refused`） |
| `3` | 未预料的运行期错误 | 照片损坏、manifest 半截等异常，已兜成一句人话 |
| `4` | **跑完了但零产出** | `build` 本次一个字都没入库 —— 补字单还空着、**或自由字版没切出任何字形**（拍空白纸/过曝）。判据看结果本身（`glyphs == 0`），与走哪条切分路径无关 |
| `5` | **跑完了但有内容没落地** | `render` / `render --flow` 超页被截断。审计报告首行写明「内容被截断」；`--flow` 会指明是第几页、从原文第几行起没渲染 |
| `130` | 用户 `Ctrl-C` 中断 | — |

护栏类断言必须**实际跑一次**核对退出码 —— 只看打印信息会漏掉"信息印出来了、退出码却是 0"这种假成功。


### 7.3 性能现状

| 项 | 数据 |
|---|---|
| `vscore`（竖画评分内核） | 向量化后**平均加速 2.9 倍**，字形越大越快（11×18 的 1.3 倍 → 69×52 的 3.7 倍） |
| `lean_report`（一次倾角搜索 = 37 次旋转 + 37 次 `vscore`） | 0.052 s → **0.026 s**，提速 2.0 倍 |
| 一页 31 字渲染 | 约 16 秒，其中竖画相关约 3 秒 |

⚠️ **当前真正的性能瓶颈是竖画扶正，不是 `vscore`**。

⚠️ **但绝对耗时在这台机器上不可靠**：同一条命令重复测，离散度实测达到 **13.4 ~ 31.4 秒**（并发负载会让 PIL 旋转慢一倍以上；有一次在满载下跑出 383.9 秒的离群值）。所以只看**比例**，不看绝对秒数：

| 配置 | 实测耗时 | 备注 |
|---|---|---|
| 默认（扶正 + 微旋转 + 加长） | 15.8 ~ 24.4 秒 | 多次重复的散布 |
| `--lean-tol 0`（仅关扶正） | 4.8 ~ 7.0 秒 | 相对稳定 |
| `--rotate 0 --stretch 0 --lean-tol 0`（三项全关） | 4.9 秒 | 与只关扶正几乎相同 |

**结论**：默认约为"关扶正"的 **2.3~2.5 倍**，即扶正占**一半以上**（约 55~80%，随当次负载浮动）。`--rotate` 与 `--stretch` 合计只占约 0.1 秒，**不是**瓶颈。

原因：`lean_report` 按字形逐个调用（每字 2 次 `vary` + 末尾一次全库扫描），每次都要做 37 个角度 × 4 倍放大图的 PIL 旋转。`vscore` 向量化省下的时间远小于这笔开销。

**两条可行的提速路线（均未实施）**：
1. **逐字形缓存倾角**：倾角只依赖字形本身，可在 `load_library` 时算一次存起来（52 次代替约 300 次）。但 `vary()` 目前对**随机变形后**的字形算倾角，改成缓存原字形倾角**会改变成品外观**，需明确接受这一点。
2. **粗搜 + 峰旁细化**：步长 3° 粗扫 + 峰附近 ±2°、步长 0.25° 细化，37 次旋转降到约 15 次，且**不改变结果**（只是先定位再精修）。这条更稳妥，尚未做。

`lean_report` 只提速 2 倍，是因为另一半时间花在 37 次 PIL 旋转上。

### 7.4 补字单墨深门槛的适用边界（实测）

`--form-sheet` 靠 `FORM_INK_DEPTH = 100`（格内平均墨深）区分"手写"与"印刷"。这个门槛**依赖 `form` 生成端的灰度设定**：

| 参考字/说明文字灰度 | 空白补字单 | 写了字的补字单 |
|---|---|---|
| **200 / 165**（现有 `form` 的输出） | 干净拒绝 ✓ | 正确入库 ✓ |
| 150 / 150 | 干净拒绝 ✓ | — |
| 120 / 120 | 干净拒绝 ✓ | — |
| **70 / 70**（v0.2 之前的旧配色） | **入库 2 个字符（收的是格内参考字）** | 入库 2 个字符（仍能收到手写字） |

即：**拿旧版本 `form` 打印的补字单，在新版 `build --form-sheet` 下参考字会被当成笔迹收进去**（实测逐格平均灰 235–238，但格内最深灰 70，低于门槛 100 故判为"有字"）。

- 这道坎**无法用灰度阈值解决**：旧参考字 70 灰与"下笔很轻的手写"70 灰在像素层面同值，任何阈值都会误伤一边。可行判据只剩"位置一致性"（印刷参考字在每个格子里都落在同一相对位置、墨色方差极小，真实手写必然有抖动），需新标定，暂不做。
- **实操**：补字单一律用当前版本的 `form` 重新生成再打印，旧打印件作废；非用不可时裁掉说明区、只拍写了字的格子。
- 详细处置与实测数据见 **4.1b 节**。

## 八、下一步（按优先级）

1. **补字单的格位与 `--expect` 长度校验**（P0，见已知不足 #11）：`total = max(len(expect), cols × nrow)` 会造出 `u%04d` 伪字符并让字名整体错位，且退出码为 0。修法：`len(expect)` 不是 `cols` 整数倍时直接拒绝（或按 `len(expect)` 定格位数），并对 `len(kept) != len(expect)` 报独立的「格位漂移」告警而不是混进"还没写"。
2. **统一 `quality` 报告的统计层级**（P1，见已知不足 #12）：四处数字统一成「字符」口径，或每处都写明是字符数还是实例数。
3. **自由字版的切分基准用主簇估计**：面积/高度的离群判据改为基于最大簇的稳健估计，解决横线纸与打印抬头导致的错名。（补字单路线已不依赖它）
4. **批量渲染**：一个描述文件出多页、连续页码与纸张一致性。
5. **PDF 导出**与打包 exe（代码已兼容 `sys._MEIPASS`，尚未构建验证）。
6. **`build` 阈值去绝对值**：81px 背景滤波窗、80px 横线开运算、20px 最小面积、40px 行分组桶都隐含"字高约 40~60px"的照片尺度。修法：标记后先估中位字高，所有常数按比例推导。
7. **扶正精度与速度的取舍**（P2-C 已评估）：两级搜索被证伪，若要继续提速需换信号处理方式（如对 `vscore` 曲线做亚度插值），而不是简单降采样。

---

生成时间：2026-09-18　代码版本：`handglyph.py`（单文件，**6053 行 / 313819 字节**，v0.7.0）

> ⚠ 行数/字节数以**发行包里由 `pack.py` 自动生成的 `PACK_INFO.txt` 为准**：本节手工写这两个数已经错过三次（曾写 3221 行、3640 行、6135 行、184731 字节、317787 字节，都与实物对不上）。这里是发版时同步的一次快照。

---

## 附：本轮修复（外部审查第四轮，6 个 P0 + 7 个 P1 + 部分 P2）

> 审查报告声明"未实际运行程序，结论多来自静态推断"，故**每条都先在本机复验**才动手。
> 复验过程中报告有 3 处与实际不符（见文末"独立判断"）。

### 🔴 P0 护栏：这些错误原先是"不报错的坏结果"

| 编号 | 问题 | 复验实况 | 修法 |
|---|---|---|---|
| P0-1 | `form --kind symbols` 必抛 `NameError` | 退出码 `EXC:NameError`，产物不存在（`SYMBOLS` 常量根本不存在） | 新增 `SYMBOLS` 常量（120 个字符）；`form` 分支改为 `a.chars or SYMBOLS` |
| P0-2 | `atlas` 不带 `--fix` 只打印用法、`return 1`，不产图鉴 | rc=1、`atlas.png` 不存在 | 新增 `write_atlas_from_lib()`，两条路都先刷新图鉴；不带 `--fix` 改为 `return 0` |
| P0-3 | `build --form-sheet` 缺 `--expect` 静默产垃圾名 | **比报告更糟**：rc=0、入库名 `u00xx`、**用户写的字一个都没进** | 入口 `sys.exit(2)`；不带 `--expect` 时按纸面尺寸估行数并明确劝阻 |
| P0-4 | 非 `--merge` 的 `build` 整体覆盖 `manifest.json` | 旧 `chars` 4 字 → 新 2 字，旧 PNG 仍在磁盘成孤儿，**零警告** | 新增 `man_has_glyphs()` + 拒绝执行（返回 `refused`），给出三种正确用法；新增 CLI `--rebuild` |
| P0-5 | `_store_glyphs()` 未失效 `metrics.json` | merge 后 `mtime` 未变 | 新增 `invalidate_metrics()`，在 `_store_glyphs` / `atlas_fix` / `clean` / `prune` 四处调用 |
| P0-6 | `pack.py --no-gate` 参数解析错位 | `out_dir="--no-gate"` → `FileNotFoundError`（恰在"救火"时炸） | 改用 `argparse`；版本改从 `pyproject.toml` 读（消除双源）；输出目录不存在给 `rc=2` + 人话 |

### 🟠 P1：误杀、静默丢弃、退化兜底

| 编号 | 问题 | 修法 |
|---|---|---|
| P1-A | `plaus()` 宽高比闸门误杀单笔画字 | 实测 `一` 55×8（asp 6.88）、`丨` 8×55（asp 0.15）、`-` 30×6（asp 5.0）全判"形状可疑"。新增 `SINGLE_STROKE_CHARS` + `is_single_stroke()`，对单笔画字**跳过比例闸门**；其余字闸门 0.25/2.6 → 0.08/12.0（只拦噪点）。**读取侧只拦"残片"**，不再做形状否决 |
| P1-B | `blend()` 越界静默丢弃，`recs` 仍记账 | 返回值化为 `(是否落下, 横向裁剪比例)`；新增 `clipped` 记账 + 越界告警 + 修正对账口径（`expected` 减去未渲染数） |
| P1-C | 纸面轮廓失败时可用宽度退化为 `10**6` | 新增 `PAPER_FALLBACK = (40, 1200)`；检测到整页回退时打印告警 |
| P1-D | `atlas --fix` 在老字库上用纯数字键跨字符误改 | 老字库（无 `instances`）**拒绝**纯数字键，要求 `字符/序号` 形式 |
| P1-E | 无顶层异常兜底 + `#gap` 解析未防御 | 新增 `run()` 外壳：把未预料异常变成人话（`rc=3`，`HANDGLYPH_DEBUG` 时打 traceback）；`#gap` 非数字给提示并按 40 处理 |
| P1-F | `manifest.json` 的 `rel` 未做路径校验 | 新增 `safe_rel()`，用 `os.path.realpath` 前缀比较（**能识破符号链接绕行**，不是简单 `".."` 检查）；所有读库/删除点都过这道闸 |
| P1-G | `--cols` 可改 `build` 但改不了 `form` | `form` 补上 `--cols` |

### 🟡 P2：性能、一致性、可维护性

- **画布重构（P2-A，报告称"本轮最大性能问题"）**：原来每贴一个字都"整页 float32 → uint8 → Image → float32"搬两趟（一页 1240×1754×3 的 float32 是 26MB）。改为全程在 float32 画布上落墨，**只在 `render()` 末尾物化一次**。
- **`render` 结尾只扫本页涉字（P2-B）**：原先对**整个字库**跑 `lean_report`，改为 `{r["ch"] for r in recs} & set(lib)`。
- **`upright()` 加内容指纹缓存（P2-D）**：键为 `(tol, cap, gain, 形状, 和, 平方和)`，超 4096 清空。
- **删死代码 8 处约 122 行（P2-G）**：`otsu` / `soft_ratio` / `stroke_lean` / `_runs` / `_line_peaks` / `inner_gaps` / `make_form_geometry` / `put`。用 AST 按 span 精确删除，删前核查零残留调用点。
- **墨色统一（P2-K）**：新增 `ink_rgb()`，消除"渲染用 `INK=(32,32,36)`、入库存 38、`clean` 写回 32"三处不一致。
- **`CELL` 常量唯一化（P2-F）**：原 `108` 散在十几处、`_write_atlas` 还写成 `104`。收成 `CELL = 108` 单一真源。
- **句柄泄漏（P2-J）**：`json.load(open(...))` / `json.dump(x, open(...))` 全改为 `read_json()` / `write_json()`。CPython 引用计数让它"看起来"能跑，但 Windows 上句柄未关时对同文件 `os.remove` 会抛 `PermissionError` —— 而本程序大量"读完就改写同一个 manifest.json"。
- **清掉全部未使用参数（P2-H）**：AST 扫描确认 4 处（`audit` 的 `img`、`detect_grid` 的 `cols`/`expect_n`、`_grid_origin` 的 `span`、`prune` 的 `k`），全部删除。

### ⚖️ 评估后**决定不做**的项（附理由）

- **P2-C（`lean_report` 的 37 次旋转降为两级搜索）**：**尝试后自己证伪并回退**。粗搜（步长 3°）+ 峰旁细化的实测结果：真值 `+3°` → 逐度扫描 `0°`（误差 3.0，恰好压线过关）vs 两级搜索 `-0.25°`（误差 3.25，不过）；真值 `-5°` → 逐度 `+2°`（误差 3.0，过关）vs 两级 `+0.5°`（误差 4.5，不过）。根因：`vscore` 的峰很窄（约 ±2°），粗搜跨过真峰就落到噪声肩，细化救不回来。**不值得为省 7 次旋转付出精度**。
- **P2-I（拆包为 9 个模块 + 全局收进 dataclass）**：这是发布形态决策，不是缺陷。单文件对"发一个 `.py` 就能用"这个使用场景是优点，且拆包会大幅提高本轮回归成本。**留待有明确需求再做**。

### 🔍 独立判断：报告与实际不符的 3 处

1. `_write_atlas` **不是**死代码 —— `_store_glyphs` 在用。
2. `font_warning` / `FONT_WARNED` **不是**死代码 —— 是"该被调用但漏了"，已接进 `make_form`。
3. `mp` / `weak` / `groups` 报告点的是**函数内**未用的那一处，不能全局删（多处定义在不同函数）。

### ✅ 本轮加固的自检

- `selftest` 从 19 项扩到 **29 项**，新增的 10 条直接对应上面的护栏（见 7.2 表）。
- **新断言立刻抓到了一个我自己引入的真 bug**：P0-4 的拒绝护栏在 `_store_glyphs` 里返回了 `refused: True`，但 `build()` 重新拼字典时**漏掉了这个键** —— 结果是"拒绝信息印出来了、退出码却是 0"，脚本里 `&& 下一步` 会照常往下走。这正是 P0-4 想杜绝的静默失败换了个形态。已修（透传 `refused`）。
- **`pack.py` 增设静态闸门**：扫"模块级常量是否定义在引用它的函数默认值之前"。来由是收 `CELL` 常量时常量放到了 1530 行、而 `build()` 签名在 1252 行，**整个程序 import 就 `NameError`**。这类错在 `selftest` 里抓不到（模块内的函数根本没机会执行），必须在模块外静态扫。闸门已用"人为破坏顺序"验证过能拦（`rc=1` 且给出精确行号）。

---

## 交付前实测补记（第五轮：把包真正拿出来跑）

第四轮把 6 个 P0 + 7 个 P1 全修完了，但**只提交了代码、没把发行包打出来**。这一轮把 v0.3 包打出来、并**解压到空目录当陌生人的机器用**，结果又抓到一条：

**🔴 `build --form-sheet` 本次零入库时退出码为 0（静默失败第四种形态）**
- 现象：用一张**还没写字**的补字单走 `build ... --form-sheet --expect ...`，程序正确报告"没有一个格子里找到手写墨迹"、JSON 里 `glyphs: 0`，**但退出码是 0**。
- 后果：脚本里 `handglyph build ... && 下一步` 会照常往下走，把"一个字都没入库"当成"建库成功"。这与 P0-4 的拒绝覆盖、P0-3 的缺 `--expect` 是同一类问题的不同触发条件 —— **第四轮只堵了后两个口子**。
- 为什么第四轮的自检没抓到：原有的 30 条断言里，"拒绝覆盖"那条用的场景是**旧库非空**；而"空白补字单 + 空目标目录"走的是完全不同的分支（`build_form_sheet` 里提前 return），当时没有断言覆盖。
- **修法**：该分支返回里加 `empty: True`；`main` 分发见到 `empty` **返回退出码 4**（新码，不占用已被 `run()` 用作"未预料异常"的 3）；`run()` 的退出码约定文档同步更新。
- **顺手修掉一处上轮漏改**：`build_form_sheet` 里 `refused` 分支**又只带了 `refused` 没带 `reason`** —— 和我上轮在 `build()` 里修过的是同一个坑，这次一起补上。
- **新增断言「空白补字单（本次零入库）退出码非 0」**，并**反向验证过**：把 `return 4` 改回 `return 0`，断言精确报出 `[失败] ... 退出码 0`、rc=1；还原后 33 项全过。断言不是假绿。

**包级验收（解压到临时目录、不依赖工作区）**

| 检查项 | 结果 |
|---|---|
| `doctor`（空目录、无字库） | rc=0，依赖/字体/纸面全 `[ok]`，正确提示"还没有字库" |
| `selftest` | **31/31 通过**，rc=0，24 秒 |
| `form 永和九年岁在癸丑 -o sheet.png --cols 4` | rc=0，产出 126048 字节 |
| `form --kind symbols`（P0-1 回归） | rc=0，120 个字符，产出 191361 字节 |
| `build` 缺 `--expect` | rc=2，被拦住 |
| `atlas --lib library`（字库不存在） | rc=2 + "先跑 build" 的完整指引 |
| `pack.py --no-gate` | rc=0，19 个文件 1.35 MB |
| 夹带检查 | **确认未夹带手写笔迹**，5 张图全在 `backgrounds/` |
| 未知子命令 / `--help` | rc=2 / rc=0 |

**包内容对比（v0.2 → v0.3）**：`handglyph.py` 144930 → 184731 字节、`pack.py` 6482 → 12705、`handglyph.cmd` 1267 → 2179、`能力说明.md` 37110 → 46034、`能力说明.docx` 63896 → 71195、`README.txt` 5826 → 6788，**新增 `LICENSE`**。12 项关键修复（`CELL` 单一真源 / `read_json`·`write_json` / `safe_rel` / `invalidate_metrics` / `man_has_glyphs` / `is_single_stroke` / `--rebuild` / `refused` 透传 / `run()` 兜底）逐项核验均在包内。

**教训（已写进 `code-review-intake` 技能）**：审查者一直读的是**桌面上那份 v0.2 旧包**（18 条目，无 `LICENSE`），而修复全在 v0.3 工作区里 —— 修完不打新包，等于没修。**交付/送审必须给发行 zip 或 md5，不能只说"在工作区里改好了"。**

---

## 新增能力（第七轮：自动排版，v0.4）

**需求**：生成的页面上，字迹之外有大片空白（内容只占半页）时，希望程序自动把后续内容
提上来填掉，而不是让人看着空白手动搬。**"后续内容"指同一篇里的下一段，或下一张纸上的字。**

**做法：量 → 装 → 画（两趟）**

| 步 | 函数 | 关键决定 |
|---|---|---|
| 量 | `measure_line_span` | 只累加宽度算行数，**不落墨、不碰 picker** —— 借 `_render_line` 会推进随机状态，量一遍画一遍结果就对不上 |
| 装 | `plan_pages` | 贪心装箱（能塞就塞），不是均分。均分会把"3 行 + 1 行长行"变成每页各 2 行，长行那页照样留白 |
| 画 | `render_flow` | 按装箱结果逐页渲染，每页都是**完整的一张纸面模板**，不是半张 |

**`blank_bands`：空白段的判据**

逐行统计墨迹比例（暗于本页中位亮度 60 的像素占比 > 0.008 算"这行有字"），
连续无墨且高度 ≥ `FLOW_MIN_GAP`(300px) 的区段即为空白段。

- 为什么**不**用连通域找字：文字是逐字贴上去的，连通域会把一个字拆成几块、又会把相邻行
  连成一片，判"哪里是空白"反而失准。逐行扫既便宜又直接对应用户看到的东西。
- 为什么只用**纸面可用横向范围**内统计：把桌子/阴影算成"有内容"会让整页都判成非空。

**两个"页尾空"数字并列，是刻意的**

| 口径 | 来源 | 反映 |
|---|---|---|
| 游标尾空 | 排版游标 `used_h` | "我打算留多少" |
| 实测尾空 | `blank_bands` 的末段 | "实际留了多少" |

两者不一致 = 有行没落墨（落点出画布、或纸面量错）。并列写出，就是为了让这种不一致
一眼可见 —— 只报游标值会得出"已压到阈值内"的假结论。

**三条实测踩出来的坑**

1. **`#gap` / `#wave` 是指令不是注释**。`render()` 里它们在 `startswith("#")` 分支之前
   就被拦下；`render_flow` 若拿 `startswith("#")` 一刀切，`#gap` 会被当注释吃掉 ——
   版面少了留白，而且**不报错**（只是字挤在一起）。实测第一版就是这么丢了 50 个字
   （113 字只渲了 63）。
2. **items 里必须存增量高度，不能存累计 y**。存累计值会让贪心判据变成"累计 > 页高"，
   结果第一行就超限、**每页只放一行** —— 实测 5 行被拆成 2 页，且页尾空白反而更大。
3. **缺字扫描要跳过指令行**。不跳的话 `#gap 40` 会报"缺字：# g a p"，
   用户按这个去补字就白跑一趟。

**自检 33 项 → 37 项**，四条新断言：装箱不越界/顺序不乱/能塞就塞（三条一起测，
只测一条的话"把每页都塞到超限"也能过）、量出的行高随内容增长、成品空白能被定位。
**其中第二条是我自己写测试时才发现的真 bug**：第一版测试用了 `no_wrap=True`，
而 `no_wrap` 下行是缩小而非折行 —— 断言写错了方向，反而暴露该条件下跨度本就不该增长。

**实测（三张作业合成一篇，31 行）**

| 页 | 行数 | 字数 | 正文占 | 页尾空（游标/实测） |
|---|---|---|---|---|
| 第 1/2 页 | 22 | 320 | 1650 / 1663 px | 13 / 0 px（已压到阈值内） |
| 第 2/2 页 | 9 | 111 | 762 / 1663 px | 901 / 1032 px（仍偏大，无后续内容可提） |

**已知边界**：单页内容本身就不满时（只有 5 行），`--flow` 无能为力 ——
没有"后续内容"可以提上来。报告会如实写"仍偏大"，**不假装填满了**。

## 新增能力（第八轮：把"抖动"压到微妙档，v0.5）

**用户的诉求**（2026-09-17 原话）：

> 「这东西主要是通过改变笔画粗细，略微——注意我的用词是**略微**——调整一下旋转角度，
> 然后增加字库的容量……而不是通过改变笔画的粗细来达成这个效果的。换句话说，
> 就是你现在这个可以改变笔画粗细，这没有什么问题。但是，你这个参数太明显了，
> 就是这个粗细变化幅度太大了，你应该把它调得**非常微妙微小**。」

也就是说：**观感的第一来源是"字库容量 + 换笔迹"，粗细和旋转只是配角。**
上一版把配角做成了主角。

### 改了什么

| 项 | v0.4 | v0.5 | 为什么 |
|---|---|---|---|
| 旋转上限 `ROTATE` | 0.8° | **0.35°** | 手写倾角集中在 ±1.5° 内，0.8 在放大图里已能逐字看出"这个字转了" |
| 尺寸抖动 | 宽 0.96–1.05 / 高 0.96–1.04 | **0.985–1.015** | 等比缩放，墨量跟着面积走：1.5% 边长差 ≈ 3% 墨量差 |
| `restroke` 粗细 | 往字形**外**糊一圈边缘（alpha 0.70），再乘 1+0.16·max(f,0) | **不扩张轮廓**，只在墨迹内做浓淡：乘 1+0.06·max(f,0)，再把淡的提浓/浓的减淡 | 见下 |
| 出锋概率 | 0.75 | **0.45** | 一半以上的字不出锋，墨量分布就窄了 |
| 出锋长度 | 0.6–1.0 倍 | **0.55–0.95 倍** | 收窄，别每个字都长尖 |
| 出锋后墨量 | 不归一化 | **归一化到原量 × 1.12** | 见下（这是本轮最大的一处） |
| `--stroke` | 无此开关 | 新增（默认 1.0 = 微妙档，0 = 完全关掉） | 给人微调空间 |

### 🔴 真凶不在 `restroke`，在 `elongate`

一开始我按用户的话只去压 `restroke`，压完发现实测还是 **12.28%** 的墨量差。
于是写了个逐步诊断（`_wb_diag.py`），逐层累加量墨量，结论出人意料：

| 步骤 | 两 seed 累计相对差 | 本步新增 |
|---|---|---|
| restroke | 7.32% | +7.32 |
| + warp | 4.49% | −2.83 |
| + **elongate** | **12.13%** | **+7.64** |
| + 旋转 | 12.25% | +0.12 |

**旋转只贡献 0.12 个点，`elongate` 贡献 7.64 个点 —— 是粗细的两倍多。**
再单独量 `elongate` 的八个方向：

| 方向 | 墨量增幅 |
|---|---|
| 水平 (0, ±1) | +18.3% |
| 垂直 (±1, 0) | +24.6% |
| 对角 (±1, ±1) | +28.1% ~ **+32.3%** |

**光方向选择就造成 14 个百分点的差。** 用户看到的"一片字有粗有细"，
绝大部分来自这里，而不是来自他说的"粗细"。

**修法**：`elongate` 算完按 `目标墨量 / 实际墨量` 整体缩一次（目标 = 原墨量 × 1.12）。
缩的是**整体不透明度**，外形分毫不改 —— 出锋的尾巴还在，只是淡下去，
正好就是真实书写里"收笔那一带墨越来越干"的样子。

| | 均值 | 标差 | 极差 |
|---|---|---|---|
| 归一化前 | 316 | 30 | **40.8%** |
| 归一化后 | 311 | 24 | **28.4%** |
| （关掉出锋作对照） | 300 | 15 | 26.3% |

### 🔴 `restroke` 里那个"整字变浓"的隐藏 bug

新写法乘 `1 + 0.06·max(f, 0)`，本意是"只有正的噪声才加浓、避免整体变浅"。
但实测单遍跑下来墨量 **−6.8% / +7%** 地单边偏 —— 这不是"有浓有淡"，是**整字一起变**。

原因：噪声场是 `rng.uniform(-1,1,(3,3))` 升采样来的，理论上零均值，
但**只有 9 个随机点、又过了一遍高斯**，落到具体一个 35×34 的字形上时均值可以偏到 +0.1。
`max(f, 0)` 再把负的一半全砍掉 → 剩下的全是正的 → 整字变浓。

**修法：`f = f - f.mean()`。** 一行，单步贡献从 7.32% 掉到 2.80%。

### ⛔ 一条被我自己写崩的断言

第一版"粗细扰动"断言量的是**两遍互比**（seed=1 vs seed=2 的墨量差）。
它把 `elongate` 的方向效应完全放过去了 —— 两个种子一平均差不多，
但**行内忽粗忽细**才是用户看到的东西。

改成量**同一 seed 内连写 40 次的离散度**（这正是"一页字里有的粗有的细"的口径），
上限 32%，实测 27.5%。**教训：断言的取样口径必须对应"用户看到的那个量"，
不然它会一路绿灯放行真问题。**

### 自检 37 项 → 39 项

| 新断言 | 防的问题 |
|---|---|
| 抖动幅度停在微妙档（粗细/旋转不被调大） | 这组数值没有客观正确值、只能靠观感判断，观感判断很容易在下次改别的东西时被顺手调大 |
| 粗细扰动实测低于 32%（行内不显胖瘦） | 常量之间的相互作用 ≠ 直觉，必须实测兜住 |

两条都是**上限**判据：允许往下调（更微妙总是安全的），只拦"涨回去"。

### 本轮为什么值得记进文档

因为这轮**用户给的判断是观感结论，而我第一刀的落点是错的**。
用户说"粗细变化幅度太大"，字面指向 `restroke`；实际主因在 `elongate`（出锋的方向效应），
两者差了三四倍。**只按字面改会改不到位**——压完 `restroke` 从 12.28% 变成 12.38%，
几乎没动。真正解决问题的是"写个诊断脚本逐层量"这一步。

## 实战补记（第六轮：拿自己的作业跑一遍）

这一轮不再看代码，而是**真的拿三张手写作业去渲染**（布尔代数：真值表对照、第 3/4/5 题化简）。跑出来的结果：**公式主体全部渲染成功**，同时抓到一条只在真实数据上才暴露的渲染缺陷。

**🔴 墨量过低的字形被放大成实心黑块**

- 现象：字库里 `·` 的入库字形是 **18×11、只有 47 个墨点像素**的一片残笔（切分时只框到了一小段）。`render` 贴字时的缩放倍率是把字形**拉到目标字号那么高**（`tw = 字形宽 × size / 字形高`），于是这个小碎片被放到 40px 高、横向拉成 65px —— 47 个墨点铺满整格，成品上就是一坨实心黑。三张作业里 `·` 出现频繁，所以这个缺陷非常显眼。
- 为什么之前没被发现：`shape` 子命令**查不出来** —— 它只走 `squeeze_report`，而那个函数明确**只查汉字**（字母、符号的手写比例与印刷体差得远，用字体宽高比当参照必然误报，实测 `-` 被判偏窄 2.37 倍而它完全正常）。`·` 是标点，走不进这条判据，于是坏字形一路静默通过。
- **修法①（渲染侧，治本）**：新增 `GLYPH_MIN_INK = 90` 与 `_too_thin()`，`_render_line` 在贴版前检查**墨量绝对值**；低到不够、且该字符有程序画点兜底（`DOT_FALLBACK`）时退回画点，否则照常跳过。判据写成通用的墨量检查而不是给 `·` 打补丁 —— 这不是"某个标点坏了"，而是"**过小的源字形被过度放大会变成墨块**"，换成任何一个字都成立。
- **修法②（检测侧）**：新增 `thin_report()`，`scan_shapes` 对非汉字改按墨量绝对值查；`shape_lines` 分两类报告（被压扁 / 墨量太低）。**阈值与渲染侧共用同一个常量** —— 两处各写一个数，改了一处另一处就漏，"shape 报了警但 render 照贴"这种自相矛盾的状态很难查。
- **⚠ 我自己在这里写错过一次，被新加的断言抓出来**：`thin_report` 的守卫我最初写的是 `if not ch.isascii(): return None`，可 **`·` 是 U+00B7，并不是 ASCII 字符** —— 它连同汉字一起被挡在门外，而这个函数存在的意义恰恰就是抓它。改成判 CJK 区段（`_is_cjk_char`）后通过。**这条说明"新增判据后必须真的跑一次"，光看代码看不出这种错。**
- **自检 31 项 → 33 项**，两条新断言都取**两端**判据：明显过小的必须被判出，正常字形必须不被误伤 —— 只测一端的话，把阈值写成 `inf` 也能"通过"。

**实测结果（真库、真作业）**

| 项 | 作业1 | 作业2 | 作业3 |
|---|---|---|---|
| 渲染字数 / 合格 | 113（合格 110） | 163（合格 159） | 175（合格 168） |
| **字数对账** | **相符** | **相符** | **相符** |
| 走程序画点的字 | 3（即 `·`） | 2 | 2 |
| 成品字节 | 1.67 MB | 1.70 MB | 1.71 MB |

- **`·` 修复已生效**：三张图里 `·` 都改走程序画点，成品上是干净的小点（对照修复前是黑块）。
- **上划线 `~X` 全程正常**：`Ā B̄ C̄ D̄` 逐行正确绘出。
- **报告"不合格"的只有 `A`、`B` 两个字母（糊 N 处）**：这是**源字形本身笔画偏重**，不是渲染问题 —— 属于"字库质量"而非"程序缺陷"，处置方式是重写这两个字，不是改代码。如实记在这里，不粉饰成"全部合格"。
- **缺字如实报出**：`STDERR: 缺字：E G`。程序没有静默跳过，而是明确告知缺什么 —— 这正是设计意图。

**这一轮的结论**：程序"能用"是**实测出来的**，不是推断出来的。三张作业里公式主体（含上划线、异或、中点、括号嵌套、多行对齐）全部正确渲染，唯一的缺口是需要补 5 个字形（`E G 不 同 相`）和重写 2 个偏重的字母。

**附带修掉一个仓库卫生问题**：上一轮提交时，本机调试脚本（`.wb_*`）被一起提交进了仓库 —— `.gitignore` 里的 `_*` 只在**文件名首字符是下划线**时匹配，**盖不住点号开头**的 `.wb_*`。补了显式规则并把那批移出索引。这类"忽略规则看着写了、其实没覆盖到"的坑，和上面 `isascii()` 那条是同一类：**规则写得像那么回事，但没有实测过它到底挡不挡得住。**

## 上一轮修复记录（外部审查第三轮，均为实测确认）


**🔴 补字单格线周期误检 —— 会造成静默污染字库**
- 现象：`detect_grid` 用自相关找格周期，对**非格线的强周期**同样敏感 —— 说明文字行距就是现成的反例（实测误检成 40/50/60 px，真值 108）。兜底只在**完全检不到**时触发，**检错了没人拦**。
- 加重因素：交叉校验只在**行列都检出**时才比较；单边检出时错周期无人制衡。且原判据用严格 `>`，周期恰为 1.5 倍（实测 60 对 40）时两个错周期一起存活。
- 后果（实测）：窗口被压成 108×50，字被拦腰切断，**残片通过墨深门槛与像素数下限被静默命名入库** —— 输出显示"入库 2 个字形 / 2 个不同字符"，看起来完全成功。入库字形实墨高 48，正确的应是 56。
- **修法①（治本）**：新增 `GRID_PERIOD_LO/HI = 0.75/1.35` —— 表单是程序画的、格宽必然是 cell，实测周期落在 `cell × [0.75, 1.35]`（81~146 px）之外一律否决、回退内置几何。修后 `cp/rp` 均为 None，入窗 108×108，字形实墨高恢复到 56/51，与"强制正确几何"结果逐值一致。
- **修法②（兜底）**：新增 `FORM_FRAG_SPAN = 0.35` 残片闸门 —— 格内墨迹高度跨度 < `0.35 × cell` 判为残片丢弃并提示。**判据锚在已知格宽上，不是窗口高**：实测"跨度/窗口高"是 0.48 对 0.50（只差 4%，能否拦住取决于误检周期碰巧落点），"跨度/格宽"是 0.19 对 0.52（两倍多余量）。
- 交叉校验的 `>` 改 `>=`。
- `selftest` 新增 3 项：带外周期被否决、入库字形不是残片、残片闸门拦住切断墨迹。**19 项全过**（最矮字形实墨高 72，下限 38）。
- 遗留（**修法覆盖不到，已如实写进文档**）：**旧版打印件**（v0.2 之前）参考字是 70 灰、比门槛 100 还深，实测一张没写字的旧表单会吐出两个完整参考字（与 53×58、门 30×57）。**这道坎无法用灰度阈值解决**（旧参考字 70 灰与"下笔很轻的手写"70 灰像素同值），可行判据只剩"位置一致性"，需新标定，暂不做。处置：旧打印件作废重打。

**文档修正**
- 更正**我自己上一轮写错的性能数字**：原写"扶正占约 80%"，来自单次测量。本轮重复测得同一命令离散度达 13.4~31.4 秒（满载时甚至 383.9 秒），**绝对耗时不可靠**，已改为只看比例（默认约为"关扶正"的 2.3~2.5 倍，扶正占一半以上）。
- 补字单旧版打印件风险在 4.1b / 7.4 / `README.txt` / 代码注释四处写明。
- `selftest` 项数 16 → 19。

v0.2 前序修复（第二轮审查）：
- 新增 `build --form-sheet` 格子感知切分；`form` 说明文字/参考字改浅灰
- `build`/`extract` 应用 EXIF 方向；文本一律 `utf-8-sig` 读
- 长行折行（`--no-wrap` 保留旧行为）、`>` 缩进、`--drift` 基线漂移、排版去重（一次布局一次绘制）
- 标点优先用字库字形；audit 字数对账
- `atlas --fix` 改名逃生门 + `manifest.instances` 实例档案
- `extract --backend auto|tesseract|paddle`；`pack.py` 出厂自检门禁

v0.1 修复：
- `#` 注释行不再被渲染进成品；超出页面的内容不再静默丢失（告警 + 写入审计报告）
- `--merge` 实例编号改为取磁盘最大序号 +1，避免 prune 删出空洞后覆盖幸存实例
- `--replace` 改为「先全部入库成功、再统一替换」，新字形被丢弃时旧字形不再连带丢失
- `--rotate` 此前是**死参数**（只赋值从不读取），已补上整字微旋转实现
- `coverage` 与 `render` 的字符跳过集合统一，标点不再被误报为缺字，`。` 归入程序画点
- 建库 alpha 由灰度反转改为**局部背景差分**：实测纸面灰雾从 0.108 降到 0.000（暗侧由 0.215 降到 0）
- `vscore` 向量化：平均提速 2.9 倍，`lean_report` 提速 2.0 倍，结果逐值一致
- 新增 `doctor`（环境自检）与 `selftest`（回归断言）；`handglyph.cmd` 改为优先挑装了依赖的解释器

## 本轮修复（第十轮：印刷格线粘连 + 字库分批 + 质检误报，v0.5.1）

这一轮用户提了三件事，**都必须先做完才允许更新文档**（用户原话："你把这一步实现之后，再去更新那些其他内容"）。三件事各自独立、成因各不相同。

### 🔴 一、每个 `A` 底下多出一根小横线（用户直接看图抓到的）

**现象**：用户原话「我看这个 A，每个一下面都有一个小横线，那是怎么多出来的？是哪里出了问题？」

**根因**：字库里 `A` 的字形（`glyphs/65/00.png`）在**入库时把采样单的印刷格线一起抠进来了**。证据链：

1. 逐行数 `A` 的墨点：`y=55` 只有 6 个，`y=56/57` **突然各 46 个**（铺满整格宽 46px）；
2. 回采样单原图确认纸面有等距印刷横线（间隔约 78px）；
3. 全库扫描，8 个字形中毒。

**修法：新增 `_cut_bottom_rule()`** —— 切掉与主体**粘连**的底部横线。

⛔ **关键是判据**。第一版用"占字宽比 ≥ 85%"，结果 8 个候选**全部 100% 命中**（含 `与`/`个`/`确` 这些底部本来就有真横画的字），差点切掉真笔画。改用**"溢出量"**（命中行的横向跨度 − 主体的横向跨度）：

| 字形 | 溢出量 | 判定 |
|---|---|---|
| `A` 底部印刷线 | 9~11 px | 切 |
| `得` 真手写底横 | 0 px | 放过 |
| `联` 真手写底横 | 5 px | 放过 |

⚠️ 另一个坑：**从底往上扫不能一上来就 break**。字形最底部有 1~2 行完全空白（裁边留的），`rows[y] == 0` 不满足满宽条件 → 第一轮就 break → 永远扫不到横线。必须先跳过尾部空白行。

⚠️ `clean()` 里**原有**删格线逻辑抓不到这个 case：它要求横线是**孤立连通域**（`ww2 >= 0.80 * width and hh2 <= 4`），而 `A` 的线**跟两条腿粘连**（整个字只有一个连通域）→ `find_objects` 拿到整字的框 → 高度不满足 → 永远删不掉。这是典型的"功能看着有、实际不覆盖这个 case"。现改为在 `clean()` 里叠加 `_cut_bottom_rule`，**从源头修掉**，以后入库自动切。

**实测效果**：切了 6 个字（`A`/`与`/`个`/`时`/`9`/`确`），逐个放大对照确认无误；糊的告警 **20 处 → 2 处**（`A` 从 10 → 2）。

### 🟠 二、字库分批与批间粗细比对（用户新需求）

用户原话：「字库有可能新补充一批，跟原来的字库它的轻重就不一样，但是新补充的字库里面的字之间一般它的轻重是一样的，这个我们存储字库的时候，解决方式可以设置一个分批，就知道它到底是什么时候存储的。然后比对一下跟之前的库里粗细的差异，如果真在一个不可接受的范围内，就让他重写一下之前的，跟这些新的一起重写。」

**实现**（详见 4.5）：`manifest.json` 新增 `batches` 字段（`{id, src, date, chars, stroke_med}`），实例档案加 `batch` 字段；`--merge` 开新批次、`--rebuild` 清空重来；新增 `_batch_stroke_median` / `compare_batches` / `batch_report` 三个函数与 `batch` 子命令；`build()` 末尾自动提示批间差异；超限时给重写清单与命令。

**三个设计要点**：
1. **批与批比中位数**，不是"全库取一个中位再逐字比" —— 后者会把一整批的偏移摊成"每个字都异常"，看不出真正的问题；
2. **粗细先折算到统一字高再比** —— 渲染是把字形拉到目标字号那么高，直接比原尺寸半径会把"字形大小差异"误当成"笔画粗细差异"；
3. **只提示、不自动改** —— 程序改粗细是缩放笔画，会把字弄得不像是本人写的；重写才能根治。

**⚠ 一条我自己写错的断言**：第一版造"第二批粗 45%"用 `2.0 → 2.9`，结果算法报 18.4% 而非 45% —— 因为基准取的是两批的**中位**（2.45），2.9 只偏高 18.4%。断言差点因为我把口径想错而红。改成 `2.0 → 4.0`（基准 3.0、偏差 ±33%）。

**⚠ 老库升级**：桌面字库是在分批功能之前建的，没有 `batches` / `instance.batch`。**不能**直接跑 `build --merge` 让它自动登记 —— `build` 是解析照片建库的，会对这份手工修过（切过印刷线、换过补字裁片）的库重新抠字形，等于把修好的字形覆盖掉。改用 `gen_out/swap3/s6_charge.py` **只补元数据、不动任何一张 PNG**：按实例来源把 52 字归 `b1`、后补的 `E G 不 同 相` 归 `b2`，再用与 `handglyph.stroke_radius` 同式的距离变换量各批中位半径。

实测结果：`b1` 中位 0.912 / `b2` 中位 0.693，偏差 ±13.6%，**在±20% 容忍带内**，无需重写。（顺带印证了分批功能的实际价值：后补那批确实比旧库细，只是还没细到要重写的程度。）

### 🟠 三、出图质检把整块拉丁字母误报成"糊"

**现象**：成品上 22 处"不合格"，但图看着是好的。报警集合 `= A B E ⊕` **全是拉丁/符号，汉字 0 个**。

**排查过程（这轮最花时间的一段）**：我**连着三轮模拟都对不上实际告警** —— 只算贴版 → 预测 `·相-→`；加上旧的 `stroke_normalize` → 预测 `-EF·→同相`；而实际是 `BEA⊕=`。差别在于渲染里还有 `vary()` 的 warp/thickness、`LATIN = 0.95`、`ink_normalize`、picker 轮转等环节。

**最后用插桩解决**：把 `_render_line` 包一层，把每次 append 的 `rec` 原样截下来喂给真正的 `audit()` —— 取到的就是**渲染器自己眼里的数**，不再靠读代码推。**教训：模拟复现复杂渲染管线，比自己插桩取真值更容易出错；对不上就先怀疑自己的模拟。**

**真因**：`dens` / `sharp` 这两个量**随字号系统变化**，而拉丁字母被 `LATIN = 0.95` 缩到 0.95 倍字号渲染 —— 笔画在小一号的框里相对更宽 → `dens` 系统性偏高、`sharp` 系统性偏低。全页取一个中位当基准，等于**拿两类不同尺度的字互比**。实测同一页 407 字：

| 字类 | 字数 | dens 中位 | 越过 dens 判据 |
|---|---|---|---|
| 汉字 | 7 | 0.277 | 3 |
| 字母数字 | 237 | 0.239 | **17** |
| 符号 | 163 | 0.191 | **0** |

**字母组 17 个越线、符号组 0 个** —— 两组字数相近（237 / 163），差异不可能来自字写得好不好。

**排除"字本身坏了"的四条证据**：
1. 逐个查被报的字形：`E`（77×129，墨点 2582）放大看**干净完整**，无残笔、无格线；
2. 全库 57 字按渲染口径复算，预测集合与实际的**对不上** → 报警依赖"页面里有哪些字"，不是字形固有属性；
3. 同一字形在不同尺度下测：`C` 与 `c`（同形同尺寸）给出**完全相同**的 `dens`/`sharp`（0.137 / 0.714）→ 纯几何量，与"是谁写的"无关；
4. 完整链路分组后：汉字组 0 个越线、字母组 17 个 → 分组能干净摘掉误报。

**修法**：新增 `_audit_class()`，分 **cjk / latin / punct** 三组，各组用**自己的中位**当基准；某组不足 4 字时退回全页基准（1~2 字时中位就是它自己，永远判"正常"）。

⚠️ 分类边界取 **CJK 码点区**，不是 `isascii()`：`⊕`（U+2295）、`→`、`·` 都不是 ASCII，但渲染尺度与标点相同；按 `isascii()` 一刀切会把它们归进"汉字组"当基准 —— 实测 `⊕` 就是这么混进去的。（**这与第六轮 `·` 踩的是同一类坑：`isascii()` 在这个项目里反复误导，凡涉及"是不是汉字"一律用 `_is_cjk_char`。**）

**实测效果**：第 1 页报警 **15 → 9**、第 2 页 **7 → 3**，且剩下的全是**组内真离群**。审计报告改为逐组印基准值与判据值，用户能看出"这个字是跟同类字比的"。

### 四、补字单提示语加"粗细要一致"（用户要求）

用户原话：「你把那个提示词让他写的时候粗细要一致就可以了。」

`make_form()` 的采写规范新增一条：**「每个字的笔画粗细要尽量一致（落笔轻重要均匀），不要有的字粗、有的字细，也不要一个字里一半粗一半细」**。

这是**从源头**减少批内不齐的手段 —— **程序端不再做任何粗细修正**（试过三版全删，见文末），真正齐整只能靠写的时候一致。
（⚠ v0.7 更正：上面这句里的实现已经换掉了 —— 旧的"整字等比缩放"在管线里被完全抵消、
等于没生效；现在是默认关闭的亚像素定向调整，见 4.6 与文末 v0.7 修复记录。）

⚠️ 顺带修掉一个隐患：新增条目时原来靠 `tips.insert(3, ...)` **硬编码索引**，数组一增减就错位；改成按内容查找插入位置（找"按平时速度"那条）。行距也改为自适应（条目 ≤10 时 40px，否则 34px）。

### 自检 39 项 → 56 项

| 新断言 | 防的问题 |
|---|---|
| 底部粘连的印刷格线被切掉（老版连通域判据抓不到） | 用户直接抓到的缺陷，且老判据对这个 case **完全无效** |
| 真手写底横不被误切（溢出量为 0 时放过） | 第一版判据差点切掉真笔画 —— 只测一端的话，判据写宽了也"通过" |
| 笔画半径按字高折算（大字形不被误判为粗） | 忘了折算的话，两个同样粗但字号不同的字形会被误判成差 3 倍 |
| 批间粗细：一致时不报、明显不齐时报 | 只测"能报出差异"的话，一个**永远返回超限**的实现也能通过 —— 那是误报 |
| 批不足 2 个时不比对 | 老库无批次信息时不能崩、也不能谎报"有差异" |
| merge 会开新批次、实例带批次号、同源的字粗细一致 | 只测纯函数的话，`_store_glyphs` 里漏写 `batches` 也照样全绿 |
| rebuild 后批次清空重建（不留幽灵批次） | 留幽灵批次会让下次比对凭空多出一批不存在的字 |
| 批间超限的报告里给出重写清单与方法 | 只返回 bool 的话，CLI 会打印"有问题但不说怎么办"的报告 |
| 质检基准按字类分组（正常拉丁字不再被误报） | 用户看不到的度量 bug，报告误导用户去重写没坏的字 |
| 分组后组内真离群仍被抓到（判据没被废掉） | 只测"不再误报"的话，一个"分组后永远判正常"的实现也能通过 |
| （对照）单一基准会把正常拉丁字一起误报 | 证明分组**确实在干活**，而不是"碰巧这批数据本来就正常" |
| 字类边界：非 ASCII 符号归符号组、不混进汉字组 | `isascii()` 在本项目反复误导（`·`、`⊕` 两次踩坑） |

**两条对照类断言的写法值得记下**：分组断言我一开始只测"越线数应为 0"，结果发现合成数据造得太温和（旧口径也不越线），断言等于白测。改成**造出"组内正常、跨组越线"的数据**（拉丁正常档 0.36、符号压到 0.19 把全体中位拉到 0.190），旧口径误报 12 处 > 新口径 2 处 —— 这样才真正证明分组有效。**只测"修好了"不测"修之前真的坏"，断言可能一直在测空气。**

### 本轮实测交付

| 产物 | 尺寸 | 说明 |
|---|---|---|
| `作业1/2/3(v0.5.1).png` | 1280×1700 / 1280×1700 / 1279×1706 | 照片换笔迹，缺字 0 |
| `作业-自动排版-第1/2页.png` | 1240×1754 | 白纸自动排版；第 1 页 301 字、第 2 页 111 字 |
| `作业-自动排版.audit.txt` | — | 质检报告（按字类分组后报警 12 处） |

全部 56 项自检通过。`A` 底部横线在**字库层面**确认已消失（最宽行 17 点 vs 改建前备份的 46 点），三张作业与两页排版图逐个放大核对无回潮。


---

## 本轮新增（第十一轮：自己导入背景，v0.6.0）

用户原话（**这条同时定了"做什么"和"不做什么"**）：

> 「让他能够自己添加背景，但是必须说明添加的背景只能是纸面，不能带有其他东西，
> 不然就会出现'字体贴到桌子上'这种情况。就是我们可以允许他自己导入背景，然后你加一个这个。
> **你不用去添加导入检验啊，不要做这个东西。你就把这个提示词设计的好一点**，
> 告诉他需要添加什么样的背景，如果添加其他的，可能会出现什么问题，那就可以了。」

### 做了什么

`render --paper` 从"只接受 5 个内置名"放开为"**内置名 或 一张图片的路径**"。判据是**这个参数能不能当图片文件打开**（扩展名在 `.png/.jpg/.jpeg` 里、且文件存在），不是"名字里有没有斜杠" —— `--paper 我拍的纸.png` 与 `--paper "D:\纸\扫描.png"` 都支持。

配套两处：
- **`choices=` 去掉**。原来是 `choices=[...]`，自定义路径会被 argparse 直接拦掉；放开后原来的错误分支 `PAPERS[a.paper]` 会 `KeyError` 崩，**所以错误分支必须重写**（见下）。
- **`doctor` 里打印提示语**。用户不需要先踩一次坑才知道有这功能。

### 没做什么（刻意）

**不做纸面自动检测。** 用户明确说了不要。设计理由也站得住：机器判"这是不是纸面"必然有误判，而误判的代价是"用户明明有可用的图却不让用"—— 这比"让用户自己看一眼"更糟。**护栏全部放在提示语里**。

### 提示语 `paper_bg_notice()`

写成**一个函数**，`doctor` 与三条报错路径**共用同一份** —— 两处各写一遍，迟早说法不一致。

内容分四块：① 该用什么样的图（整幅是纸 / 正对拍 / 光线匀 / ≥1500px，并明确"淡格线没关系"）；② 不要什么样的图（桌面、手掌、书本边缘、笔、水杯）；③ **用错了会怎样**；④ 一句话判据。

第 ③ 块是重点，**两种后果都实测过**：

| 后果 | 触发条件 | 程序报警？ |
|---|---|---|
| 字只写在一角、大片空白 | 图里混进桌面/手掌 → 那块被判非纸面 → 可用宽度变窄 | ❌ **不会**（轮廓量出来了，程序以为正常） |
| 字跑到纸外、贴到桌子上 | 纸面轮廓**压根量不出来** → 退回保守宽度 `PAPER_FALLBACK` 硬排 | ✅ 会告警，但那时已经排坏了 |

实测复现：造"左侧 2/3 横线纸 + 右侧 1/3 木桌面"的图跑 render，正文只占左侧纸面，右半边整页空着，**无任何告警** —— 正是第一栏。这条如果不写进提示语，用户拿不到任何线索。

### 三条报错路径（原先会崩）

| 输入 | 处理 |
|---|---|
| 内置名但背景文件缺失 | "应在 backgrounds/ 下放 X.png"，指路 `doctor` |
| 图片路径但文件不存在 | "检查路径有没有写错、文件是不是被移走了" |
| 既非内置名、也非图片 | 列出可用内置名 + "也可以填图片路径，但必须整幅都是纸" |

三条都**跟着打印一遍完整提示语** —— 出错时正是最需要读到它的时候。

### 自检 56 项 → 58 项（第十一轮：自定义背景）

| 新断言 | 防的问题 |
|---|---|
| 自定义背景：内置名与图片路径互不误判 | 只测一侧的话，"任何输入都当路径"或"任何输入都查内置"都能过。测了 7 个点：内置名仍走 backgrounds/、真图片原样返回、未知名回 None（不抛异常）、内置名不被当路径、真图片被认出、不存在的路径不认、`.txt` 不认 |
| 自定义背景提示语：说清了该用什么图、用错会怎样 | 提示语是本功能**唯一的护栏**（刻意不做校验）。少了"字贴在桌子上"这类话，用户拿带桌面的照片进来就会中招 |

### 实测

- `selftest` **58/58 通过**
- 自定义背景端到端跑通：自建 `1240×1754` 纸面 → 出图尺寸与内置纸一致
- 对照跑"干净纸 vs 带桌面"两张图，肉眼确认差异（即上表第一栏）
- 三种错误输入分别 rc=2、文案正确、无 traceback
- `doctor` 输出排版正常（提示语缩进与项目其他段落一致）

---

## v0.7 修复记录（外部审查第六轮：对 v0.6 **发行包**的复审）

v0.6 的复审结论是：新功能方向对、判据标定讲究，但**"接入点"缺端到端断言** —— 三处高严重度问题全都是"函数测透了、接进管线才失效"。本轮按优先级全部处理，并把这批缺陷对应的断言补成**管线末端断言**（量成品像素、退出码、报告文字、命中率）。

### 🔴 一、`stroke_normalize` —— 三版全废，最后整块删除

这一项前后翻车三次，最后不是"改对了"，是**确认这件事做不到、把功能删掉**。三段都留着，因为每一段都是一种"看着对、实测不成立"的典型。

**第一版（v0.6 及以前）：被管线完全抵消**

- **现象**：`_render_line` 里 `a, sk = stroke_normalize(a, stroke_target)` 之后，**`sk` 从未被读过**，`size_eff = float(size)` 与 k 无关。
- **机理（几何恒等式，不需要跑）**：整字等比放大 k 倍 → 高 H·k、半径 r·k；随后无条件重采样到**高度恰好 = size**（相对 a 的系数 1/k）。净变换 `k × 1/k = 1` —— 外框、笔画宽度、字距全部原样回来，只多两次重采样。也就是说那段注释里"笔画仍偏粗/偏细"的说法是**错的**。
- **为什么长期没发现**：三条断言都是**对函数单测**（细笔画不被放大 / 粗笔画仍能放大 / 缩放不污染浓淡），函数本身没问题；失效在**接入之后**，而 `pyflakes` 也看不见"元组解包绑定未使用"（`sk` 就是这一类）。
- **顺带**：`page_stroke_target` 的折算分母原来写成常量 40（`r * (ref_h/40)`，而 ref_h 恰好也是 40 → 系数恒为 1 = 没折算），与 `batch` 侧的 `r * (40/字形高)` 口径不一致。现两处共用 `folded_stroke_radius()`。

**第二版（v0.7 初版）：改成推等值面，仍然恒等**

- **做法**：距离变换取有符号距离场，把 0.5 等值面外推/内收 `delta` 像素，`delta = (want - f)·H/BODY_H`，限幅 `STROKE_NORM_DELTA_CAP = 0.35` px。
- **实测**：两条断言直接失败（`墨量 102.0 → 加粗 109.4 / 变细 102.0`；`贴版半径 1.111 → 1.111`）。诊断脚本（`gen_out/v07_in/_sn_diag*.txt`）测出根因：
  - EDT 给的是**整数格点距离**（1.0 / 1.414 / 2.0…），**没有亚像素信息**；
  - `clip(0.5 + sdf + delta)` 要改动**任一**像素，delta 得把某个像素推过 0.5 那一档，而"紧贴边缘那圈背景像素"的 `-sdf + 0.5` **恒等于 1.5**；
  - 于是 delta 在 `[-0.5, +0.5]` 内**全是恒等变换**，真字形要 delta≈**1.4** 才动 —— 而 cap 是 0.35。
- **教训**：**"数学上能连续取值"和"离散数据上真的会变"是两件事，必须量。**

**第三版（本轮）：双路径改造 → 判定做不到 → 删除**

- **改造**：细笔画（半径 <1.5px）走形态学 `binary_erosion/dilation`、实心笔画走等值面，delta 统一从 `(want-f)·H/BODY_H` 解。
- **实测结论（`gen_out/v07_in/_sn_check.txt`）——三条都指向"不可行"**：
  1. **细笔画"变细"完全动不了**：1px 条腐蚀一次就整条消失，加了"墨 <8 点就停手"保护后等于不动（墨量 40 → 40）。不是代码不努力，是**退无可退**。
  2. **粗笔画只能整格跳**：`dilation` 一次一整圈。实测条宽 6/10/16 加粗时墨量 384→448、640→704、1024→1088，涨的全是"宽 1px"的量。
  3. **量尺在细笔画上没有分辨率**：2px 条与 3px 条的 `stroke_radius` **都是 1.0000**。所以"半径纹丝不动"里头很大一部分是**量尺看不出差别**，不是墨没变（条宽 4 加粗时墨量 160→200 真变了 25%，半径读数照样不动）。**这也意味着原来把"半径朝目标移动"写成断言，本身就不成立。**
- **用户决定（2026-09-18）**：**直接删掉这个功能**。做法是
  - 删 `stroke_normalize` / `page_stroke_target` 两个函数、4 个 `STROKE_NORM_*` 常量、`--stroke-norm` CLI 参数、`_render_line`/`render`/`render_flow` 三处 `stroke_target` 形参与传参、`globals()` 里的两行赋值；
  - `stroke_radius` / `folded_stroke_radius` **保留**（`batch` 的批间一致性报告还在用，那是"人写得多稳"的唯一量化出口），但 docstring 补上"只做报告、不喂修正"与"细笔画上分辨率为零"两条限制；
  - 4 条假绿断言删掉，换成 3 条**钉住删除依据**的断言：`已彻底移除（无残留入口）` / `1px 笔画腐蚀一次即消失` / `2px 与 3px 量出同值`。这样将来有人想加回来，会先在这里被拦下。
  - 常量区原位置留一段长注释写明**三层原因**，文档（README / 本节）同步改成"程序不管粗细"。
- **教训（最贵的一条）**：**测的东西不像真东西，测过也不能算数。** 前两版的断言样本是一根人造大竖条（源半径 4.5），真实字库笔画半径多在 1~2px —— 判据在样本上成立、在真实数据上从根上不成立。

### 🔴 二、`--flow` 装不下时**静默丢内容**

- **现象**：`render_flow` 里 `truncated` 只被置真然后 `break`，之后再没被读过：那一页剩余的行**既不在本页、也不在任何后续页**（分页第一趟已定好），而审计报告还写着"已压到阈值内"。
- **后果**：v0.1 修好的"超页不静默丢失"在新路径上复活。
- **修法**：记录 `(页码, 原文行号, 没渲染的行数)` → stderr 告警 + 写进该页审计首行 + `render_flow` 返回 `trunc_pages` → CLI 以**退出码 5** 结束。单页 `render()` 同样改为返回截断标志并给出 rc=5（此前只在报告里写、退出码仍是 0）。

### 🔴 三、`measure_line_span` 系统性低估行宽 10~22%

- **现象**：估算用一张固定倍率表（汉字 0.98h…），而 `_render_line` 的实际推进量是 `字形宽 + 字距`，**估算表里没有字距这一项**（字距 = h×0.22×(0.85~1.2)）。
- **后果**：量出来的行数偏少（每 5 行少算 1 行）→ 分页偏乐观 → 撞上"截断"。两条老断言（装箱不越界 / 跨度随内容涨）都是**自洽性检查**，测不出这个偏差。
- **修法**：抽出 `token_metrics()` —— 量行高与贴字**共用同一份宽度口径**（字形真实宽高比 + 字距期望值 × `GAP_EST_MARGIN = 1.10` 保守余量）。新增**跨模块断言**：同一行文字，量出的行数与实际贴出的行数差 ≤1（旧实现在这条下会失败）。

### 🟠 其余

| # | 问题 | 修法 |
|---|---|---|
| 4 | `write_atlas_from_lib` 输出**黑底白字反相图**（alpha 当灰度画 + `paste` 不传 mask） | 墨色 = `(1-a)` 反相、形状 = `a` 当 mask；老字库（无 `instances`）在图鉴底部标明"红字是遍历顺序" |
| 5 | `_too_thin` 只处理标点兜底，**"跳过并说明"这条路径不存在**（文档却写着有） | 非标点字形墨量过低时**跳过**，记一条 `skipped` → 审计标「跳过」并点名，附处置命令 |
| 6 | `rec["clipped"]` 只写不读 | `audit()` 汇总：几个字形被裁掉过半、最狠多少、涉及哪些字 |
| 7 | `lib_quality` 把"metrics.json 损坏"报成"**字库为空**" | 读不到就 `calibrate()` 重算，只有真算不出来才报空库 |
| 8 | `--cols` / 字数无上限校验 → 格子画到纸外、`build` 端当空位跳过（**静默少字**） | `make_form` 按 `FORM_W/FORM_H` 算列/行上限并拒绝；**按 `copies` 展开后**的格数算 |
| 9 | `atlas --fix` 之后图鉴仍是**改名之前**的状态 | 改名成功后自动重刷图鉴 |
| 10 | `build()` 内部 `sys.exit(2)`（库函数杀进程） | 改为返回 `refused`，由 CLI 层翻译成退出码 |
| 11 | `--stroke` 的 4 个全局量**按当前值派生** → 同进程调两次变 k²、k³ | 从只读基准 `_STROKE_*0` 派生；新增幂等断言 |
| 12 | `SINGLE_STROKE_CHARS` 靠隐式字符串拼接，**全角引号 `“”` 根本没进集合** | 改显式分段 + 转义；新增"含全角引号且 ≥40 个"断言 |
| 13 | 死代码 8 处（含 `drift` 让 `compare_batches` 白算一次全库扫描、`sk` 这种 pyflakes 看不见的） | 全清；`pack.py` 接入 `pyflakes` 门禁 + 包内自动生成 `PACK_INFO.txt`（字节数不再手抄） |
| 14 | 文档：`selftest` 项数写法不一、"其中 1 处条件执行" | 改为如实写法：**70 处** `check()` 调用、部分嵌在守卫里、**实际执行数随环境变化** |

### ✅ 自检 58 项 → 70 处（实际执行 69 条）

新增 13 条，全部落在**管线末端**：`图鉴底色亮暗比` / `倾角缓存命中率` / `自由字版零入库 rc≠0` / `--flow 装不下时 rc=5 且报告写明截断` / `裁切过半进报告` / `墨量过低标跳过` / `坏 metrics 重算` / `补字单容量（含 copies）` / `字符集含全角引号` / `--stroke 幂等` / `笔画归一方向与外框` / `贴版半径折算口径` / `量行高与贴字口径一致`。

### ⚠ 本轮**未做**的验证（必须说清楚）

- 本轮改动是在**没有 `pillow/numpy/scipy` 的环境**下完成的：只做了 `py_compile` 语法编译、`pyflakes` 零告警、两道打包门禁（常量顺序 / 死代码）实测返回空，**没有运行过 `selftest`**。
- 新增的 13 条断言按代码语义推导写成，**首次运行以实测为准**；若失败，先看是哪一条、再对照本节定位。
- 第 3 项（`stroke_normalize`）**已整块删除**，对默认输出无影响（它本来就默认关）；第 4 项（图鉴）改变 `atlas.png` 的外观（这才是修复目标）。
- `能力说明.docx` 未同步（二进制文档），需要时由本 `.md` 重新导出。
